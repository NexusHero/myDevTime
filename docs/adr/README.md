# Architecture Decision Records

Significant, hard-to-reverse decisions, captured so the _why_ survives the people who made it.
Format is a lightweight [MADR](https://adr.github.io/madr/): **Context → Decision → Consequences**
(+ **Alternatives considered** when the choice was close), plus a status. Numbering is sequential
and immutable; a decision that replaces another **supersedes** it rather than editing history.

This is a hard rule, not a suggestion: a PR that introduces or swaps a technology, or changes a
cross-cutting architectural pattern, does not merge without a new or updated ADR in the same
PR — see [`CONTRIBUTING.md`](../../CONTRIBUTING.md#ways-of-working) and
[`skills/ultimate-dev-process/SKILL.md`](../../skills/ultimate-dev-process/SKILL.md) §1.2.

## Writing one

Copy the newest file as a starting point, take the next free number, and keep the MADR shape: a
short **Context** (the forces at play), the **Decision** in one or two sentences, and
**Consequences** (what this enables, what it costs, what it forecloses). Add the row to the table
below in number order.

| ADR | Decision | Status |
|-----|----------|--------|
| [0001](0001-adopt-ultimate-development-process.md) | Adopt the Ultimate Development Process (via sibling project Finanzo) as this project's governance model | Accepted |
| [0002](0002-product-scope-unify-tyme-and-tackle.md) | Product scope: unify Tyme's mobile/tablet UX with the second reference's AI automation & billing, plus an own AI layer — iOS + Android + Web from 1.0 | Accepted, amended by 0008 (reference = Tactiq; transcription in 1.0) |
| [0003](0003-node-typescript-backend.md) | Node.js/TypeScript backend as a modular monolith | Accepted |
| [0004](0004-react-native-expo-client.md) | React Native + Expo client for all three platforms | Accepted (provisional) — confirmed by spike [#1](https://github.com/NexusHero/myDevTime/issues/1) ([findings](../spikes/0001-client-rn-expo.md)); residual on-device checklist (C1–C7) before the qualifier drops |
| [0005](0005-deterministic-core-llm-assist.md) | Deterministic tracking/billing core; LLM strictly an assist layer with recorded provenance | Accepted |
| [0006](0006-subscription-billing-stripe-plus-store-iap.md) | Subscriptions via Stripe on web + native IAP in stores, unified by an internal entitlement service | Accepted, amended by 0008 (explicit AI credits) |
| [0007](0007-authentication-email-oauth-sessions.md) | Email/password + Google & Apple sign-in, rotating token sessions, self-hosted auth module | Accepted — session mechanism amended by 0017 (opaque DB sessions); provider list extended by 0018 (+GitHub) |
| [0008](0008-tactiq-realignment-transcription-and-credits.md) | Scope update: Tactiq (not Tackle) as second reference; meeting transcription in 1.0; explicit AI-credit billing | Accepted — amends 0002 & 0006 |
| [0009](0009-meeting-capture-asr-approach.md) | Meeting-capture channel & ASR provider — decision frame fixed, winner pending the capture spike ([#31](https://github.com/NexusHero/myDevTime/issues/31)) | Proposed — pending the capture spike |
| [0010](0010-attendance-absences-signable-report.md) | Scope update: attendance (punch clock, breaks, overtime), absences (vacation/sick/holidays), signable PDF+XLSX work-time report — all 1.0 | Accepted — extends 0002/0008 |
| [0011](0011-ai-co-planner-and-design-language.md) | AI Co-Planner in 1.0 (plan entity, deterministic planner, ghost-block proposals) + binding UX vision with a prototype gate before component code | Accepted — extends scope; adds gate #39 → #11 |
| [0012](0012-competitive-feature-adoption.md) | Competitive adoption: focus mode + streaks, idle/forgotten-tracking detection, calendar write-back, Jira/Linear/Slack insight export — all 1.0 | Accepted — extends 0002/0008/0010/0011 |
| [0013](0013-competitive-adoption-round-2.md) | Competitive adoption round 2: entry notes, month overview with gap markers, budget burn-down forecast, Siri/App-Intents + Quick Tile, classic day list, Zeitausgleich absence type | Accepted — extends scope; next scope ADR must displace, not add |
| [0014](0014-monorepo-toolchain.md) | Monorepo toolchain: pnpm workspaces, Vitest (+ v8 coverage gate), ESLint flat type-checked + Prettier, one `test.sh` gate = CI, git hooks | Accepted |
| [0015](0015-backend-framework-and-persistence.md) | Backend framework & persistence: Fastify modular monolith (plugin-per-module) + PostgreSQL + Drizzle; RFC 7807 errors; generated OpenAPI | Accepted (realizes 0003) |
| [0016](0016-cicd-pipeline.md) | CI/CD pipeline (mirrors the Résumé project): commitlint, security (audit + dependency-review), CodeQL, Dependabot, tag-driven release, GitHub Pages OpenAPI mirror | Accepted |
| [0017](0017-auth-implementation-library.md) | Auth implementation: **Better-Auth** (self-hosted, MIT); focused libraries (openid-client + argon2 + @fastify/\*) kept as documented fallback; integration validated as acceptance criteria in #4 | Accepted — realizes & amends ADR-0007 |
| [0018](0018-social-providers-and-auth-edge.md) | Social providers **Google + Apple + GitHub** (Facebook rejected); Better-Auth hidden internally but exposed at the client edge on purpose | Accepted — extends ADR-0007 |
| [0019](0019-sync-protocol.md) | Cross-device sync: server-authoritative delta sync with per-entity optimistic versioning; deterministic per-entity-type conflict policy in `packages/domain` (LWW for catalog metadata, user-surfaced conflicts for time-entry intervals & delete-vs-edit) | Accepted — realizes the `sync` module (0003); engine is #9 Phase 2 |
| [0020](0020-export-rendering-stack.md) | Timesheet export/rendering: hand-rolled CSV, ExcelJS (XLSX), PDFKit (PDF) behind per-format serializer adapters over the deterministic `buildTimesheet`; server-side, `Intl` de/en formats | Accepted — realizes REQ-009's rendering layer (#14) |
| [0021](0021-task-effort-estimation.md) | Task effort estimation: deterministic category→hours-range baseline + user's own estimate + assist-only AI estimate review (never mutates the number) + estimate-vs-actual | Accepted — grows 1.0 (REQ-041, #90); no displacement, by explicit owner choice |
| [0022](0022-three-accent-themable-design-system.md) | Themable design system: two-axis tokens (accent × mode), three swappable accents (Sovereign / Ember / Blueprint) on shared neutrals; a11y contract across all six combinations | Accepted — extends ADR-0011; realizes #11's token/component foundation; **default accent superseded by 0023** |
| [0023](0023-blueprint-default-accent.md) | Default accent → **Blueprint** ("Königsblau" `#2563EB`); Sovereign/Ember stay first-class, user-selectable; token architecture & a11y contract unchanged | **Superseded by ADR-0061** (default → Sovereign, v11 refresh); supersedes the default-accent choice of ADR-0022 |
| [0024](0024-backend-dependency-wiring.md) | Backend dependency wiring: manual constructor injection (factory modules over typed `deps`) + Fastify plugin/decorator composition; DIP via narrow ports, not a container | Accepted — **superseded by 0025** (owner chose NestJS DI) |
| [0025](0025-adopt-nestjs-on-fastify.md) | Adopt **NestJS 11 on `@nestjs/platform-fastify`**: modules/controllers/providers + container DI, `nestjs-zod` validation/OpenAPI, RFC-7807 exception filter, Better-Auth guard; Fastify kept as HTTP layer; `packages/domain` stays pure | Accepted (owner decision) — supersedes 0024 & the composition/DI half of 0003/0015; resolves spike [#104](https://github.com/NexusHero/myDevTime/issues/104) |
| [0026](0026-design-system-pro-tier-adjustments.md) | Design system pro-tier adjustments: density tokens (regular/compact) on `Theme`, 12-color categorical project palette, absolute (non-`rgba`) soft semantic colors for deterministic WCAG contrast across surfaces, a micro logo variant | Accepted — extends ADR-0011/0022/0023 |
| [0027](0027-mobile-ui-testing-strategy.md) | Mobile UI testing strategy: `@testing-library/react-native` + `react-test-renderer` component tests for `apps/mobile`, run by the same Vitest runner (ADR-0014) via a JSDOM environment and native-module mocks at the app boundary | Accepted — extends ADR-0014 |
| [0028](0028-rest-over-graphql-api-style.md) | Client↔server API style: RESTful resource endpoints (not GraphQL/tRPC); composed reads get purpose-built aggregate endpoints; a read-only GraphQL/BFF facade stays available additively if ever needed | Accepted — makes explicit the API-style dimension of ADR-0003/0015/0025 |
| [0029](0029-llm-provider-port.md) | Provider-agnostic **`LlmPort`**: one narrow interface (`complete`/`available`, structured output + token usage), vendor SDK types confined to per-provider adapters (`openai`/`anthropic`/`gemini`/`ollama`); provider chosen by config; a `NullLlm` default so AI degrades gracefully — LLM proposes only (ADR-0005) | Accepted (owner decision) — the port that unblocks the AI layer (REQ-013/014/015/026); real adapters land with the features |
| [0030](0030-comprehensive-design-system.md) | Comprehensive, production-grade design system: expanded token scale (spacing s0–s8, typography 2xs–3xl, motion timing + easing, borders, app-shell geometry), integrated branding assets (logos, splash, favicon), font loading (Blueprint: Inter/JetBrains/Space Grotesk, Sovereign/Ember: system), deterministic project colors via FNV-1a hash, all components token-driven, WCAG AA verified across six theme combinations | Accepted — realizes & extends ADR-0011/0022/0023/0026; closes design-system spike, unblocks full-fidelity UI implementation across all platforms |
| [0031](0031-coplanner-llm-labeling-port.md) | Co-Planner **LLM garnish** behind a narrow `PlanLabeler` port owned by the planner: the LLM only ranks/labels the code-enforced blocks (never places time), sharing the `PlanLabel` shape with the pure deterministic fallback so it degrades gracefully; one credit debited only when the AI actually ran, idempotent per plan, priced at the controller/composition layer | Accepted — realizes the deferred ADR-0011 garnish clause (REQ-031/#151) over `LlmPort` (0029) + credit ledger (0008) |
| [0032](0032-connector-token-vault.md) | **Connector token vault**: per-user/workspace OAuth access+refresh tokens for dev/chat/calendar integrations stored **encrypted at rest** (envelope encryption + AEAD, KMS-sealed in prod), behind a narrow `TokenVault` port; refresh + rotate + revoke; workspace-isolated; keys never in source, tokens never logged; crypto backend confined to one adapter | Accepted (owner decision) — secret-storage foundation for the connectors layer; KMS backend a Trial detail |
| [0033](0033-connector-scopes-and-consent.md) | **Connector scopes & consent**: least-privilege scopes per capability (read-only by default, write only on opt-in), consent stored **per connector and per capability** (inbound/outbound/capture), preview-before-write, revocation+erasure into the DSGVO flows, per-provider data-processing matrix, audit trail — generalises consent-first (REQ-025) to all integrations | Accepted (owner decision) — permission foundation for the connectors layer; per-provider scope sets filled as adapters land |
| [0034](0034-signal-palette-refresh.md) | **Signal-palette refresh** (post-user-test): Ember accent amber `#E8A33D` → vivid signal orange `#ff5320` (white ink; light `accentText #b33009` for AA), `live` punched up to `#ff5320`/`#ff6b3d` (`liveStrong #e33e0f`/`#ff5320`), `good` brightened to `#16a34a`/`#4ade80`; values only, no architecture, a11y contract still green across all six accent × mode combos | Accepted (owner decision) — supersedes the Ember/`live`/`good` values of ADR-0022 (via 0026/0030); first slice of the July-2026 design refresh |
| [0035](0035-bounded-screens.md) | **Bounded screens**: a page's scroll depth must not grow with the user's workload (design v1) — density shows as fill level, not scroll depth. Pure `boundedList(items, limit, expanded)` primitive (top-N + "+N weitere" drill-in) in `packages/design`; first consumer `ProjectsScreen` (flat list sorted by budget risk, bounded + show-more, per-client grouping retired). Presentation only | Accepted (owner decision) — extends ux-vision §2/§3; viewport-locked shell + Today/Reports consumers follow |
| [0036](0036-first-run-onboarding-gate.md) | **First-run onboarding gate** (design v3): the Welcome → Arbeitszeit → Projekte → Auto-Tracker → Fertig flow shown once per device via a tiny `onboardingStore` seam (`localStorage` on web, in-memory fallback on native) behind `hasOnboarded()`/`markOnboarded()`; `OnboardingGate` sits inside `AuthGate` and seeds synchronously (no flash). Captured answers not yet persisted; presentation + one boolean flag, no deterministic-core impact | Accepted (owner decision) — extends ux-vision §5 and ADR-0035; server-side flag + answer persistence follow |
| 0037–0039 | _Unused — reserved for the closed PRs #172/#173 (offline SQLite / performance); their work is superseded by ADR-0040/0041._ | — |
| [0040](0040-offline-first-local-store-as-sync-client.md) | **Offline-first local store as the sync client**: the device's local SQLite is the *client half* of ADR-0019, not a new architecture. One schema for both modes (carries `workspace_id`/`version`/`updated_at`/`deleted_at`/`device_id`/`operation_id`); the store is a **thin repository** with **no math** — every offline number comes from the same `packages/domain` functions the API uses (offline == online, ADR-0005); standalone = sync-off + synthetic workspace, team = sync-on (ADR-0019 engine). Workspace-scoped by construction | Accepted (owner decision) — extends ADR-0019, bound by ADR-0005; supersedes the offline-only approach of PR #172; delivered via epic #174 (#175–#177); **the offline-first line is retired by ADR-0049** |
| [0041](0041-reanimated-ui-thread-timer.md) | **UI-thread stopwatch (react-native-reanimated)**: the live clock ticks on the UI thread via a display-only `ReanimatedTimer` (Today hero + Island) instead of a per-second `setState` that re-rendered whole screens; `useTimer` drops the tick and exposes raw inputs, source of truth + `formatStopwatch` unchanged (presentation only, ADR-0005 unaffected); Vitest aliases reanimated to a shim (ADR-0027). Cherry-picks the one sound idea from #173 | Accepted (owner decision) — introduces `react-native-reanimated`; other #173 changes rejected/deferred |
| [0042](0042-offline-money-path-in-the-core.md) | **Offline money path in the core**: mirror the server's `rates`/`budgets` tables into the local schema (carrying the ADR-0040 sync/tenancy columns) and compute offline Reports money via `packages/domain` — a single entry-pricing rule (`rateForEntry`) reused by server and client, `priceBillableEntries`/`budgetConsumptions` over local rows. Retires the fabricated demo figures offline; a seeded default rate + budgets make the numbers real from tracked time; overtime stays an honest `0` until offline shifts exist | Accepted (owner decision) — realizes ADR-0040, bound by ADR-0005; delivered via epic #174 (#176); **retired by ADR-0049** |
| [0043](0043-adopt-powersync-for-offline-sync.md) | **Adopt PowerSync for offline-first sync**: the client uses PowerSync's managed SQLite + upload queue + streaming pull + checkpoints instead of the hand-rolled `sync_outbox`/`sync_state`/`runSync`. Writes flow through `uploadData()` to our backend, which applies them via the **existing** `packages/domain` conflict engine — so interval conflicts are still **surfaced, never auto-merged** (REQ-006). Sync rules scope by `workspace_id` (isolation). Retains `packages/domain` + the ADR-0042 offline compute; retires the #188/#189 hand-rolled client sync; SDK behind the `LocalDb` port | Accepted (owner decision) — supersedes the client half of ADR-0019 & ADR-0040; bound by ADR-0005; **superseded by ADR-0049 (offline-first abandoned)** |
| [0047](0047-tanstack-query-server-state.md) | **TanStack Query for server-state**: `@tanstack/react-query` v5 is the server-state manager for non-PowerSync reads (billing/AI/connectors/catalog). `useAsync` becomes a thin adapter over `useQuery` (callers untouched); raw `useState`/`useEffect` fetch hooks (`useConnectors`) move to `useQuery`/`useMutation`. Offline data (local SQLite / PowerSync) deliberately does **not** flow through it — two data planes. `usePlanner`'s mutation-heavy flows deferred | Accepted (owner decision) — supersedes hand-rolled `useAsync`/`useEffect` fetches; bound by ADR-0005/0043/0046 |
| [0046](0046-drizzle-on-the-client.md) | **Drizzle on the client**: `packages/local-db` repositories query SQLite through Drizzle (typed `tables.ts` + column projections) instead of hand-written SQL strings + row mappers — one query style across server (ADR-0015) and client. Drizzle runs over the **existing `LocalDb` port via `sqlite-proxy`**, so nothing new couples to a vendor driver (works iOS/Android/web, render-test null path intact). DDL stays in `SCHEMA_SQL`; a drift test keeps the two in lock-step. The schema is PowerSync's `@powersync/drizzle-driver` on-ramp (#193). Consistency change, not a bug fix; public repo signatures + `Local*` shapes unchanged (no caller touched) | Accepted (owner decision) — supersedes the raw-SQL of ADR-0040; extends ADR-0015 to the client; bound by ADR-0005; **client store removed by ADR-0049** |
| [0045](0045-expo-router-and-list-virtualization.md) | **Expo Router + list virtualization**: file-based navigation under `apps/mobile/app/` replaces the hand-rolled `AppShell`/`ScreenView` switch, driven by the retained `packages/design` nav model (`buildPath`/`parsePath`) — real URLs/deep links on every platform. Web `output: static` (SSG) improves first **paint**; honestly, Metro does **not** JS-code-split, so time-to-interactive is unchanged. `@shopify/flash-list` (via a new `ScreenListScaffold`) virtualizes the unbounded lists (credit ledger, task entry history) — the reliable perf win | Accepted (owner decision) — supersedes the #11 hand-rolled router; realizes spike #191; bound by ADR-0004/0035 |
| [0048](0048-client-motion-pass.md) | **Client motion pass (design v4)**: port the design system's v4 motion to `apps/mobile` — mount count-ups/draw-ins (BudgetRing, Gauge, Sparkline, Heatmap, LeaveBalance) via one rAF primitive `useMountValue` over a **pure** `easeOutCubic` in `packages/design`; looping live motion (Island dot pulse, `LiveButton` breathing + pulse waves) via reanimated. **Reduced-motion-first**: `useReducedMotion()` gates everything (target on first frame, no loops). MoodCheck repurposed from a standing widget to a self-dismissing **punch-out** prompt. Animated values only ease toward deterministic-core numbers (a11y labels report the true value); on-device frames verified outside the gate | Accepted (owner decision) — realizes design-system v4; extends ADR-0004; bound by ADR-0005/0027; reuses the reanimated runtime from ADR-0041 |
| [0049](0049-abandon-offline-first-architecture.md) | **Abandon offline-first**: remove the local SQLite store (`@mydevtime/local-db`), the client's dual online/offline data paths, and the backend `SyncModule` — the app is **online-only**, hooks talk straight to the API (or the labeled demo path when no API is configured). Loses offline data + local optimistic UI (managed via TanStack Query instead); keeps the installable PWA app-shell (shell cache ≠ offline data). Adds Dockerfiles (`node:22-alpine` API + `nginx:alpine` web) + `docker-compose` (Postgres/Redis/api/web) with migrate-on-start, for a local production-like test env | Accepted (owner decision) — **supersedes ADR-0040/0042/0043/0046** (the offline-first line); the deterministic core (ADR-0005) and server tracking/billing are untouched |
| [0050](0050-redis-rate-limiting.md) | **Global rate limiting (Redis-backed)**: `@nestjs/throttler` bound globally via `APP_GUARD` (100 req/min/client); storage is `@nest-lab/throttler-storage-redis` when `REDIS_URL` is set (shared counters across instances), else per-instance in-memory (tests/single node). Health probes and the Stripe webhook are `@SkipThrottle()`d; `trustProxy` for the client IP is env-gated (`TRUST_PROXY`, default off) so a directly-reachable API can't be IP-spoofed | Accepted (owner decision) — new infra dependency (Redis); groundwork for future caching/queues |

| [0051](0051-invoicing-abrechnung.md) | **Invoicing / "Abrechnung"** (design v6): turn a client's billable, un-invoiced entries in a period into a frozen invoice. Pure `packages/domain/invoicing` (`invoiceLines`/`summarizeInvoice`) prices each entry with the **same** `rateForEntry` → `costOf` math as Reports, so invoice = dashboard. New `invoices` table + `invoice_id`/`invoiced_at` markers on entries (migration 0017): a billed entry leaves the "open" pool, voiding returns it. Server-authoritative `issue` (recompute + intersect selection + stamp, one transaction); `preview`/`void`/`list`/`clients/open` endpoints under `/api/billing`, workspace-scoped. Billable € toggle reuses the existing entry `PATCH` | Accepted (owner decision) — extends ADR-0015 money layer (REQ-005/009); bound by ADR-0005 |
| [0052](0052-container-smoke-test.md) | **Container smoke test** (artifact-parity gate): a third CI tier that builds the production Docker images and black-box HTTP-smokes the running stack (Postgres · Redis · api · web/nginx) via `scripts/container-smoke.sh` — readiness `/health/ready` (proves migrate-on-boot + DB), liveness, the nginx-served SPA, and the `nginx → api → guard` edge path (401). CI-only `docker-compose.ci.yml` publishes the api port for the probe. Runs on push to `main`, `workflow_dispatch`, and PRs that touch the container/edge surface — parity without taxing every PR | Accepted (owner decision) — complements ADR-0016/0027; exercises the ADR-0049 Docker artifacts |
| [0055](0055-embed-design-fonts-in-invoice-pdf.md) | **Embed the design faces in the invoice PDF** (amends ADR-0054): render the invoice with the real design typography instead of PDFKit's Helvetica/Courier — Clash Display (title/wordmark), Inter (body), JetBrains Mono (all figures). Faces ship as `.ttf` decoded from the licensed `.woff2` (ITF Free / SIL OFL) under `export/fonts/`, copied into `dist` by the Dockerfile (like migrations) and resolved via `import.meta.url`. Presentation only — the frozen figures are unchanged (ADR-0005); a render test asserts the faces are embedded and the standard fonts are gone | Accepted (owner decision) — amends ADR-0054 |
| [0054](0054-design-system-v7-invoice-pdf-and-adherence-gate.md) | **Design system v7 — invoice PDF + adherence gate**: v7 changed no tokens/components (byte-identical to v6); it adds an A4 "Rechnung" template + an adherence lint config. Render the invoice as a real PDF (`invoice-pdf.ts`, sole PDFKit adapter) from the **frozen** `InvoiceExport` in the warm DevTime look — positions grouped per project, dark grand-total block = stored `totalMinor` (ADR-0005); `?format=pdf\|csv` on the export endpoint; `senderName` = workspace. Only data we hold is rendered — the template's mocked address/bank/tax/VAT are omitted (issuer profile is follow-up). Adopt the one RN-transferable adherence rule ("colours from the theme, not raw hex") as a **ratchet** — `check-design-adherence.mjs` + a per-file baseline in `./test.sh`, failing only on new drift; `px`/font/import rules don't map to RN. Reference refreshed to v7 | Accepted (owner decision) — extends ADR-0051/0005; complements ADR-0027 |
| [0053](0053-acceptance-e2e-and-requirements-traceability.md) | **Browser acceptance + requirements traceability**: a fourth CI tier drives the built web app through Chromium against the running Docker stack (`e2e/`, Playwright) — app mounts, seeded user signs in past the auth gate, wrong password rejected (REQ-002/007). Auth's email-verification becomes an env flag (`AUTH_REQUIRE_EMAIL_VERIFICATION`) with a **production refine forbidding it off** in prod; the `docker-compose.e2e.yml` overlay turns it off only in dev so seeded accounts log in. Plus a machine-checked `docs/testing/requirements-traceability.md` mapping every register REQ to its tests, enforced by `scripts/check-req-coverage.mjs` in `./test.sh` (no register REQ without a row; no dead test reference). Same triggers as ADR-0052 + the E2E surface | Accepted (owner decision) — extends ADR-0052; delivers the seeded-login journey it deferred; complements ADR-0027; serves REQ-022 |
| [0056](0056-kubernetes-deployment.md) | **Kubernetes (k3s) deployment on Hetzner**: declarative `k8s/` manifests (api/web Deployments, Postgres StatefulSet + PVC, Redis, traefik Ingress with cert-manager TLS) + a `Deploy to Kubernetes` workflow on push to `main` — build/push the api & web images to GHCR (`:latest` + `:sha-<7>`, lowercased repo), then apply, migrate on the **freshly built SHA image** (`envsubst`), and `kubectl set image` + rollout. Readiness `/health/ready`, liveness `/health`, `pg_isready`/`redis-cli ping`. **Secrets are out-of-band**: only `*.example.yaml` templates are tracked (`k8s/**/secret.yaml` gitignored), and `DATABASE_URL` is composed from the Postgres secret via `$(VAR)` so no password sits in a manifest | Accepted (owner decision) — complements ADR-0016/0052; deploys the ADR-0049 Docker images |
| [0057](0057-auto-tracker-activity-capture.md) | **Auto-Tracker — activity aggregation behind a capture port**: split the "app usage while tracking" feature along the deterministic/volatile seam. Pure `summarizeActivity` in `@mydevtime/domain` turns activity spans into a percentage-correct breakdown (merge, top-N "Others", largest-remainder to 100). A narrow `ActivityCapture` port isolates the volatile part: a real first-party **web** adapter (own-tab Active/Idle/Away via Page Visibility + input-idle, local-only, `SpanAccumulator` with injected clock) and an honest **null** adapter where OS capture is impossible (iOS/native today). Consent- and session-gated (`autoTracker` opt-in, only while tracking); nothing persisted or sent off-device. Real OS capture (Android UsageStats / desktop companion) is a follow-up behind the same port — coarse-but-true over rich-but-fake | Accepted (owner decision) — realizes REQ-042; bound by ADR-0005; ports & adapters (SKILL §2.2); **extended by ADR-0058** |
| [0058](0058-native-app-usage-capture.md) | **Native OS app-usage capture behind the Auto-Tracker port** (extends ADR-0057): advance real per-app tracking toward Android. A narrow `NativeUsageModule` port + a **tested** JS adapter — `diffUsage` turns the native module's cumulative per-app totals into per-interval spans (baseline-first, delta, reset-safe), `nativeUsageCapture` polls and feeds them to the same deterministic `summarizeActivity`. `platformCapture` routes Android to it **when a Dev Client build provides the module**, else the honest null adapter. The Android `UsageStatsManager` module + config is a **documented scaffold** (`apps/mobile/native/mydevtime-usage/`) — unverified without an on-device build, so shipped not-wired. iOS impossible; desktop a future companion; local-only + consent/session-gated (no server ingest) | Accepted (owner decision) — advances REQ-042; bound by ADR-0005; ports & adapters (SKILL §2.2) |
| [0059](0059-desktop-usage-companion.md) | **Desktop app-usage companion (macOS/Windows)** (extends ADR-0057/0058): a standalone Electron + `active-win` companion that captures the focused app and renders a **local** per-app breakdown, **reusing** the tested `summarizeActivity` from `@mydevtime/domain` (no duplicated accounting). Lives in top-level `desktop-companion/` — its own toolchain, **not** a pnpm-workspace member, so its Electron deps + unverified binary never enter `./test.sh`. Local-only + consent-first (app names only, nothing uploaded); a separate process, so it renders its own view rather than feeding the mobile port. Unverified in this environment — a documented build-it-locally scaffold | Accepted (owner decision) — realizes the desktop half of REQ-042; bound by ADR-0005 |
| [0060](0060-balance-card-local-only-checkin.md) | **Balance card — deterministic strain signals + local-only OLBI check-in** (extends ADR-0012): the full design-v10 Balance card on Reports. Passive half is the deterministic core's — `weeklyFocusTrend` (10-week sparkline) + `dailyHoursDistribution` (day-length box plot) join `workloadLoad`, composed by `buildBalance` from the user's own tracked time. Self-report half is a weekly **OLBI short-form** check-in (exhaustion + detachment) stored **local-only** by contract (`checkinStore`, never uploaded — burnout-adjacent Art. 9 data kept off any server, on-screen "stays on your device" promise kept). Neutral framing — a signal, never a diagnosis | Accepted (owner decision) — realizes REQ-032 (Balance); bound by ADR-0005; privacy-by-design (Art. 25) |
| [0061](0061-design-refresh-v11-sovereign-default-clash-display-live-mark-mascots.md) | **Design refresh v11 — Sovereign default, Clash Display display face, LiveMark + edge mascots** (supersedes ADR-0023's default; extends ADR-0022/0026/0030/0048): reconciles the client with the v11 design-system handoff after a review found it ~90 % already shipped. Flips the default accent **Blueprint → Sovereign** (royal blue, the flagship); wires **Clash Display** as the display face for Sovereign & Ember (Blueprint keeps Space Grotesk) with **JetBrains Mono numerals in every theme**; adds **LiveMark** (the living "Now-Split" logo dot — blinks idle, pulses tracking, jumps on punch-out; wired into the sidebar brand, reduced-motion-safe) and edge mascots **Sevi** + **Blocky** (empty states + onboarding only, never in working UI). Token-driven, no mock data | Accepted (owner decision) — v11 refresh; bound by ADR-0005; motion per ADR-0048; RN render tests per ADR-0027 |
| [0062](0062-accessibility-baseline-approach.md) | **Accessibility baseline — labelled primitives + graphical-instrument descriptions** (realizes the first slice of REQ-043): the interactive primitives already expose ARIA role/label/state via react-native-web; this closes the real gap — the SVG / coloured-bar **instruments** (Gauge, Heatmap, BoxPlot, sparklines) now announce their value as `role="img"` + `aria-label`, so a screen reader speaks the number instead of silence. Text-bearing instruments stay text-readable (no double-labelling); render tests pin each label. Contrast already enforced in CI. The **axe-core E2E gate** + a **keyboard golden-path/focus-ring** pass are the deferred next slice | Accepted (owner decision) — REQ-043 Partial; bound by ADR-0022/0030; render tests per ADR-0027 |
| [0063](0063-calendar-centric-ia.md) | **Calendar-centric IA — four places + an avatar** (supersedes ux-vision §3's five-tab/seven-item model; extends ADR-0011): the v12 handoff reworks the IA around "the calendar is the living place". The sidebar rail becomes exactly the four places (Today · Planner · Projects · Reports); **Profile is an avatar footer** ("me", not a rail item; phone keeps it as the fifth tab). **Meetings, Absence and the Assistant stop being destinations** — their content folds into the calendar via a **typed Planner entry drawer** (H2) and an **Assistant overlay** (H3), landing in follow-up PRs. Routes are untouched, so deep links / command bar / Profile hub keep every surface reachable (a nav test asserts no orphan). This PR lands the structural nav change (H1) | Accepted (owner decision) — v12 IA; app-shell rides #11/REQ-007; nav model in `packages/design` |
| [0064](0064-auto-tracker-reality-layer.md) | **Auto-tracker reality layer — timestamped local day history + deterministic drift/gap core** (extends ADR-0057/0058/0059; realizes ADR-0063 K1/K3): the current tracker persists merged-by-source session totals with no timestamps and no day dimension, wiped at session end — so the Planner can't show a reality trace, a drift chip, or heal yesterday. This adds the pure `reality.ts` core — `TimedSpan {source,startMs,endMs}`, `trackedMs` (Idle/Away excluded), `realityDrift` (signed tracked−booked), `detectUnbookedGap` (the single largest unbooked active stretch ≥ a floor, labelled by dominant source). Persistence gains a **local, per-day, timestamped** history in a follow-up — still on-device, consent-gated, **never uploaded** (0058/0059's privacy stance unchanged). Observation proposes, never books (ADR-0005) | Accepted (owner decision) — REQ-042 reality core; core in `packages/domain`; client history + K1/K3 UI follow |

## Tech Radar

One line per technology so the stack's shape stays visible without re-reading the ADR log
(process skill §1.4). Adopt / Trial / Assess / Hold.

| Technology | Ring | Decided by |
|------------|------|------------|
| Node.js + TypeScript (`strict`) backend | Adopt | ADR-0003 |
| pnpm workspaces (monorepo) | Adopt | ADR-0014 |
| Vitest (+ v8 coverage gate) | Adopt | ADR-0014 |
| ESLint flat (type-checked) + Prettier | Adopt | ADR-0014 |
| Fastify (backend HTTP framework) | Adopt | ADR-0015 |
| NestJS 11 (modules/controllers/providers + DI) on `@nestjs/platform-fastify` | Adopt | ADR-0025 |
| nestjs-zod (Zod validation + OpenAPI DTOs) | Adopt | ADR-0025 |
| REST resource endpoints (client↔server API style) | Adopt | ADR-0028 |
| GraphQL (client↔server API) | Hold (declined; read-only BFF facade re-assessable if a need appears) | ADR-0028 |
| tRPC (client↔server API) | Hold (declined — couples RN client to backend TS types) | ADR-0028 |
| reflect-metadata + emitDecoratorMetadata (`apps/api` only; SWC transform in Vitest) | Adopt (scoped to the backend) | ADR-0025 |
| Manual constructor injection (factory modules over typed `deps`) | Superseded by NestJS DI | ADR-0024 → 0025 |
| PostgreSQL | Adopt | ADR-0015 |
| Drizzle ORM (+ drizzle-kit migrations) | Adopt | ADR-0015 |
| Drizzle ORM on the client (typed local SQLite queries via `sqlite-proxy` over the `LocalDb` port) | Retired (removed with offline-first) | ADR-0046 → 0049 |
| Hand-written raw SQL + row mappers in `packages/local-db` | Superseded by client Drizzle | ADR-0046 |
| React Native + Expo (+ react-native-web, EAS) | Adopt (provisional — spike #1 passed; on-device checklist pending) | ADR-0004 |
| Expo Router (file-based navigation; web `output: static`/SSG) | Adopt (confined to `app/` + `src/shell`; drives the `@mydevtime/design` nav model) | ADR-0045 |
| Hand-rolled shell router (`AppShell`/`ScreenView` active-screen switch) | Superseded by Expo Router | ADR-0045 |
| `@shopify/flash-list` (virtualized long lists, via `ScreenListScaffold`) | Adopt (unbounded lists only — ledger, entry history) | ADR-0045 |
| `@tanstack/react-query` (client server-state; `useQuery`/`useMutation`) | Adopt (the app is now online-only — all reads flow through it, ADR-0049) | ADR-0047 |
| Hand-rolled `useAsync` / raw `useEffect` fetch hooks | Superseded by TanStack Query (`useAsync` now a thin adapter over it) | ADR-0047 |
| react-native-reanimated + Gesture Handler (Day Canvas 60fps) | Adopt | ADR-0004/0011 |
| Reduced-motion-first client animation (rAF `useMountValue` over pure `easeOutCubic`; reanimated loops for live pulse/breathing) | Adopt (mount count-ups/draw-ins + live signals; OS reduced-motion gates all of it) | ADR-0048 |
| expo-sqlite (offline-first local store) | Retired (offline-first abandoned) | ADR-0004/0019 → 0049 |
| Themable design tokens (`@mydevtime/design`: accent × mode, 3 accents; **Sovereign default** (ADR-0061); density regular/compact, 12-color project palette, absolute soft colors) | Adopt | ADR-0011/0022/0026/0061 |
| Living brand mark `LiveMark` + edge mascots `Sevi`/`Blocky` (Now-dot blinks/pulses/jumps bound to the timer; mascots in empty states + onboarding only, reduced-motion-safe) | Adopt | ADR-0061 (bound by ADR-0048 motion) |
| Calendar-centric IA — four places (Today · Planner · Projects · Reports) + a Profile avatar footer; Meetings/Absence/Assistant fold into a typed Planner drawer + Assistant overlay (routes kept for deep links) | Trial (H1 nav + Assistant overlay H3 + typed Planner entry drawer H2 + deterministic "✦ Fill week" K2 landed — tap a block for its typed detail (meeting attendance, booked-time actions, ghost accept/dismiss, break); `cascadeFreeSlots` packs the inbox into free slots as ghosts, never past/over-booked, one undo; the "● Reality" trace + drift chip K1 overlays the auto-tracker's observed activity + yesterday-healing K3 offers to book an unbooked stretch the tracker saw — the full v12 calendar-centric set (H1–H3, K1–K3) is in) | ADR-0063 (supersedes ux-vision §3 IA) |
| `@testing-library/react-native` + `react-test-renderer` (mobile component tests, run by Vitest) | Adopt | ADR-0027 |
| jsdom (DOM env for component tests that mount react-native-web `TextInput` etc., per-file `@vitest-environment`) | Adopt | ADR-0027 |
| react-native-svg (instrument viz: budget rings, balance gauge, sparklines) | Adopt | ADR-0011/0022 (ux-vision §2.5) |
| Inter · Space Grotesk · JetBrains Mono via expo-font / @expo-google-fonts (Blueprint trio) + self-hosted **Clash Display** display face for Sovereign/Ember; JetBrains Mono numerals in every theme | Adopt | ADR-0022/0061 (font-loading slice) |
| Flutter | Hold (named fallback; not triggered by the spike) | ADR-0004 |
| Stripe (Checkout + Billing) | Adopt | ADR-0006 |
| StoreKit 2 / Play Billing | Adopt | ADR-0006 |
| RevenueCat | Assess (re-evaluate when IAP work starts) | ADR-0006 |
| LLM providers (multi-provider via config) | Adopt (behind one adapter interface) | ADR-0005 |
| OAuth 2.0 connectors (GitHub App, Jira/Atlassian, Slack, Google, GitLab, Linear, MS/Teams, Zoom …) behind a `Connector` port | Adopt (the model; per-connector adapters land incrementally) | ADR-0032/0033 |
| Envelope encryption + AEAD token vault (`TokenVault` port; KMS-sealed in prod) | Adopt (backend a Trial: cloud KMS vs self-hosted AEAD key) | ADR-0032 |
| GitHub App vs OAuth App (connector auth shape) | Assess (open decision) | ADR-0032 |
| ASR providers (Whisper/Deepgram/AssemblyAI/…) | Assess (spike #31, behind `TranscriptionPort`) | ADR-0009 |
| Meeting-bot capture service (Recall.ai-style) | Assess (first candidate in the capture spike) | ADR-0009 |
| Auth SaaS (Auth0/Clerk/…) | Hold | ADR-0007 |
| argon2 password hashing, OIDC client libs | Adopt | ADR-0007 |
| Better-Auth (self-hosted TS auth framework) | Adopt (integration validated in #4) | ADR-0017 |
| Social login: Google + Apple + GitHub | Adopt | ADR-0007/0018 |
| Facebook login | Hold (rejected — weak fit for the dev audience) | ADR-0018 |
| openid-client + @node-rs/argon2 + @fastify/\* (focused-libs fallback) | Assess (documented escape hatch if #4 can't meet the criteria) | ADR-0017 |
| Lucia (auth library) | Hold (retired upstream — not a dependency) | ADR-0017 |
| Server-authoritative delta sync (per-entity versioning + tombstones) | Adopt | ADR-0019 |
| Deterministic per-entity conflict policy (in `packages/domain`) | Adopt | ADR-0019 |
| CRDT libraries (Yjs / Automerge) | Assess/Hold (revisit only for real multi-user collaboration) | ADR-0019 |
| ExcelJS (XLSX export, behind an adapter) | Adopt | ADR-0020 |
| PDFKit (PDF export, behind an adapter) | Adopt | ADR-0020 |
| Headless Chromium → PDF (Puppeteer) | Hold (heavy + non-deterministic bytes; revisit if layout outgrows PDFKit) | ADR-0020 |
| GitHub Actions CI (gate + Postgres integration + commitlint) | Adopt | ADR-0014/0016 |
| CodeQL (javascript-typescript, security-and-quality) | Adopt | ADR-0016 |
| Dependabot (npm + github-actions, grouped) | Adopt | ADR-0016 |
| GitHub Pages OpenAPI mirror (self-hosted Swagger UI) | Adopt | ADR-0016 |
| Provider-agnostic `LlmPort` (ports & adapters for the LLM) | Adopt | ADR-0029 |
| Anthropic (Claude) LLM adapter | Trial (launch rail, behind the port) | ADR-0029 |
| OpenAI LLM adapter | Trial (launch rail, behind the port) | ADR-0029 |
| Google Gemini LLM adapter | Trial (hosted hedge, behind the port) | ADR-0029 |
| Ollama LLM adapter (local/self-hosted) | Trial (privacy + free test rail, behind the port) | ADR-0029 |
| PowerSync (Postgres ⇄ SQLite offline sync) | Retired (never shipped; offline-first abandoned) | ADR-0043 → 0049 |
| `jose` (JWT/JWKS for PowerSync device tokens) | Retired (sync module removed) | ADR-0043 → 0049 |
| Hand-rolled ADR-0019 client sync (outbox/watermark/runSync) | Retired (superseded by PowerSync, then removed with offline) | ADR-0043 → 0049 |
| Redis (`ioredis`) — rate-limiter store, future caching/queues | Adopt (infra; `REDIS_URL`, in-memory fallback) | ADR-0050 |
| `@nestjs/throttler` + `@nest-lab/throttler-storage-redis` (global rate limiting) | Adopt (100 req/min; health/webhook skipped) | ADR-0050 |
| Docker Compose local env (Postgres + Redis + api `node:22-alpine` + web `nginx:alpine`, migrate-on-start) | Adopt (local production-like testing) | ADR-0049 |
| Container smoke test in CI (build prod images + black-box HTTP checks against the running stack) | Adopt (artifact-parity gate; main/dispatch/container-touching PRs only) | ADR-0052 |
| Kubernetes (k3s on Hetzner) as the deploy target, GHCR images, traefik + cert-manager, push-to-`main` rollout | Adopt (single-node, not HA; secrets out-of-band) | ADR-0056 |
| Auto-Tracker via a capture port: pure aggregation core + first-party web adapter (own-tab activity), honest null adapter elsewhere | Adopt (consent-gated, local-only; native OS capture a follow-up) | ADR-0057 |
| Native Android app-usage capture (`UsageStatsManager`) behind the same port: tested JS diff/adapter now, native module as a documented Dev-Client-only scaffold | Trial (adapter tested; native module unverified without an on-device build) | ADR-0058 |
| Desktop app-usage companion (macOS/Windows) — standalone Electron + `active-win`, reusing the deterministic `summarizeActivity`; local-only | Trial (aggregation reused/tested; Electron companion a build-it-locally scaffold) | ADR-0059 |
| Auto-tracker **reality core** — timestamped `TimedSpan` + `realityDrift` (signed tracked−booked) + `detectUnbookedGap` (single largest unbooked active stretch); feeds the Planner reality trace / drift chip / yesterday-healing | Adopt (core + durable per-day local history + the **Planner "● Reality" trace + drift chip** K1 + **yesterday-healing** K3 landed — a consent-gated banner offers to book the largest ≥15-min stretch the tracker saw yesterday but that was never booked, adopt/dismiss once a day, honest-empty) | ADR-0064 (extends 0057) |
| Balance card — deterministic strain signals (`weeklyFocusTrend`, `dailyHoursDistribution`) + weekly OLBI self-report stored **local-only** by contract (never uploaded); a signal, never a diagnosis | Adopt (passive signals tested in the core; check-in on-device — no server surface for Art. 9 data) | ADR-0060 |
| Playwright browser acceptance (`e2e/`, drives the built web app vs the running Docker stack) | Adopt (acceptance tier; app-mount + seeded sign-in; main/dispatch/app-touching PRs) | ADR-0053 |
| Machine-checked requirements traceability (`check-req-coverage.mjs` in `./test.sh`) | Adopt (every register REQ ↔ tests; no dead references) | ADR-0053 |
| Invoice PDF export (PDFKit adapter, design v7 "Rechnung", frozen figures) | Adopt (REQ-005/009; `?format=pdf`) | ADR-0054 |
| Embedded design fonts in the invoice PDF (Clash Display · Inter · JetBrains Mono, .ttf shipped into the image) | Adopt (real brand typography) | ADR-0055 |
| Design-adherence ratchet (`check-design-adherence.mjs`: no new raw-hex colours in `apps/mobile`) | Adopt (guardrail; per-file baseline burn-down) | ADR-0054 |
| Accessibility: ARIA-labelled primitives + graphical instruments announce their value (`role="img"` + `aria-label`); WCAG-AA contrast enforced in CI | Trial (REQ-043 baseline; axe-core E2E gate + keyboard/focus pass pending) | ADR-0062 |
