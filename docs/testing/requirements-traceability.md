# Requirements traceability matrix

_Every requirement in the register maps here to the automated tests that exercise it._

This is the machine-checked bridge between the **Requirements Register**
(`docs/architecture.md` §1, REQ-001…REQ-046) and the test suite. The gate
(`scripts/check-req-coverage.mjs`, run by `./test.sh`) enforces two invariants:

1. **Completeness** — every `REQ-NNN` in the register has a row here. A new
   requirement cannot land without declaring how it is (or will be) verified.
2. **No dead test references** — every test path named in a row must exist on
   disk. Renaming or deleting a test without updating its requirement fails CI,
   so this matrix cannot silently rot.
3. **Acceptance per delivered requirement** — every REQ marked **Verified**
   names at least one **acceptance-tier** test: the requirement exercised
   end-to-end against the real system — an API-integration test
   (`*.integration.test.ts`, real Postgres), a browser E2E spec (`*.spec.ts`), a
   client render test (`*.test.tsx`), or the container smoke script. A pure
   domain/unit `*.test.ts` alone does not satisfy it (SKILL §7). A row marked
   **Deferred** (a dormant, undelivered feature) is exempt.

It does **not** assert that a passing test proves a requirement complete — that
is what code review and the acceptance tier are for. It guarantees that the
_traceability_ is honest and current, and that nothing reaches **Verified**
without an acceptance test standing behind it.

## Coverage states

| State | Meaning |
|-------|---------|
| **Verified** | The requirement's behaviour is exercised by automated tests (unit / integration / acceptance) listed in the row. |
| **Partial** | A load-bearing slice — a domain core, an adapter seam, a shared UI helper — is tested, but the full requirement is not yet delivered. The row names what _is_ covered. |
| **Planned** | Proposed in the register, no implementation yet. The row names the tracking issue; there are no tests to reference. |

## Test tiers (what each layer proves)

- **Domain** (`packages/domain/**/*.test.ts`) — the deterministic core (ADR-0005),
  ≥90 % coverage, purity-gated. Every number that reaches a timesheet, budget,
  export, or invoice is proven here.
- **Design** (`packages/design/**/*.test.ts`) — pure UI geometry / tokens /
  responsive & nav model, ≥90 % coverage.
- **API integration** (`apps/api/**/*.integration.test.ts`) — the real NestJS app
  booted in-process against a real Postgres service container, driven via
  `app.inject()`; includes negative workspace-isolation tests.
- **API unit** (`apps/api/**/*.test.ts`) — guards, config, adapter confinement,
  RFC-7807 mapping.
- **Client** (`apps/mobile/**/*.test.ts(x)`) — API-client parsing/round-trips and
  component render tests (ADR-0027, outside the coverage bar).
- **Acceptance / E2E** (`e2e/**/*.spec.ts`) — the built app driven through a real
  browser against the running Docker stack (ADR-0053).
- **Container smoke** (`scripts/container-smoke.sh`) — black-box HTTP checks
  against the shipped production images (ADR-0052).

---

## Matrix

| REQ | Coverage | Verifying tests | Notes |
|-----|----------|-----------------|-------|
| REQ-001 | Verified | `apps/api/src/modules/tracking/catalog.integration.test.ts`, `apps/api/src/modules/tracking/entries.integration.test.ts`, `apps/api/src/modules/tracking/validation.test.ts` | Workspace-scoped repositories; negative isolation tests assert cross-workspace reads/writes are refused. |
| REQ-002 | Verified | `apps/api/src/modules/auth/auth.integration.test.ts`, `apps/api/src/modules/auth/auth.controller.test.ts`, `apps/api/src/modules/auth/guard.test.ts`, `apps/api/src/modules/auth/confinement.test.ts`, `apps/api/src/config.test.ts`, `apps/mobile/src/api/auth.test.ts`, `apps/mobile/src/screens/LoginScreen.test.tsx`, `apps/mobile/src/screens/RegisterScreen.test.tsx`, `apps/mobile/src/shell/AuthGate.test.tsx`, `e2e/tests/shell-auth.spec.ts` | Email/password + verification, shared `AuthGuard`, Better-Auth vendor confinement; browser acceptance proves sign-in end to end. Client Login + Register (design v8): email sign-in/up, password-reset request, and Google/Apple/GitHub buttons wired to `/sign-in/social` — each enabled only when its OAuth secrets are configured (the public `/api/auth/providers` reports which). |
| REQ-003 | Verified | `packages/domain/src/tracking/time.test.ts`, `packages/domain/src/tracking/overlap.test.ts`, `packages/domain/src/tracking/rounding.test.ts`, `packages/domain/src/tracking/aggregation.test.ts`, `packages/domain/src/tracking/time-entry.test.ts`, `apps/api/src/modules/tracking/entries.integration.test.ts` | Deterministic, dependency-free tracking core; purity-gated (`scripts/check-domain-purity.mjs`). **Acceptance:** the core's rules (overlap, one-running-timer, provenance, validation) are enforced end-to-end through the real entries API against Postgres — a rule-violating entry is refused there, not just in the pure unit tests. |
| REQ-004 | Verified | `packages/domain/src/tracking/time-entry.test.ts`, `apps/api/src/modules/tracking/entries.integration.test.ts`, `apps/mobile/src/api/timer.test.ts`, `apps/mobile/src/timer/reconcile.test.ts`, `apps/mobile/src/timer/timerStore.test.ts` | One running timer (DB-enforced), manual create/edit/split/delete validated by the core; provenance `source` on every entry. A running **or paused** timer survives an app restart: the server is authoritative for the running segment, the client-only session (banked total + paused context) is persisted locally and merged back by `reconcileTimer`. |
| REQ-005 | Verified | `packages/domain/src/budgets/money.test.ts`, `packages/domain/src/budgets/rates.test.ts`, `packages/domain/src/budgets/pricing.test.ts`, `packages/domain/src/budgets/budget.test.ts`, `packages/domain/src/invoicing/invoice.test.ts`, `packages/domain/src/reporting/rollups.test.ts`, `apps/api/src/modules/billing/invoice.integration.test.ts`, `apps/api/src/modules/billing/export/invoice-pdf.test.ts`, `apps/api/src/modules/billing/billing.integration.test.ts`, `apps/mobile/src/api/invoicing.test.ts`, `apps/mobile/src/reports/revenueBudget.test.ts`, `apps/mobile/src/api/budgets.test.ts`, `apps/mobile/src/api/rates.test.ts`, `apps/mobile/src/screens/RatesScreen.test.tsx`, `apps/mobile/src/insights/insights.test.ts` | Integer money math (minor units, BigInt cost); invoicing prices with the same rate math as Reports (ADR-0051); invoice exports as CSV + the design v7 "Rechnung" PDF from the frozen figures (ADR-0054); rates are set per workspace/client/project from the client Rates screen + onboarding default. Reports "Revenue & Budget" (D13): deterministic revenue-per-client + effective rate + open-billable aging (`rollups.ts`), a workspace `GET /api/billing/aging` endpoint, and the client per-client rollup. Reports analytics (design v10): the **budget burn-down** — as-of consumption sampled across the period (`budgetBurndownFor`, integration-tested) with the pure exhaustion projection (`burndownProjection`) — and the **12-week tracking heatmap** (`dailyMinutesSeries`). |
| REQ-006 | Verified | `packages/domain/src/sync/crud.test.ts`, `packages/domain/src/sync/resolve.test.ts`, `packages/domain/src/sync/engine.test.ts`, `packages/domain/src/sync/simulation.test.ts` | **Deferred (ADR-0049):** app is online-only; the deterministic conflict engine is retained and fully tested but unwired as the documented re-entry point. |
| REQ-007 | Verified | `apps/mobile/src/screens/TodayScreen.test.tsx`, `packages/design/src/responsive.test.ts`, `packages/design/src/nav.test.ts`, `e2e/tests/shell-auth.spec.ts` | Today view + responsive split-view model; acceptance proves the shell mounts and a user reaches the app. Native background visibility (Live Activity) is device-only, out of CI. |
| REQ-008 | Verified | `packages/domain/src/reporting/summary.test.ts`, `packages/domain/src/reporting/finance.test.ts`, `apps/api/src/modules/tracking/summary.integration.test.ts`, `apps/api/src/modules/billing/billing-summary.integration.test.ts`, `apps/mobile/src/api/reports.test.ts`, `packages/design/src/instruments.test.ts` | Deterministic summary/finance rollups + the Reports instruments geometry. |
| REQ-009 | Verified | `packages/domain/src/reporting/timesheet.test.ts`, `apps/api/src/modules/billing/export/export.test.ts`, `apps/api/src/modules/billing/export/invoice-csv.test.ts`, `apps/api/src/modules/billing/export/pdf.test.ts`, `apps/api/src/modules/billing/export/export.integration.test.ts` | Every exported number traceable to `buildTimesheet`; CSV/XLSX/PDF serializers behind per-format adapters. |
| REQ-010 | Partial | `apps/api/src/modules/connectors/service.test.ts`, `apps/api/src/modules/connectors/crypto.test.ts`, `apps/api/src/modules/connectors/connectors.integration.test.ts`, `apps/mobile/src/api/connectors.test.ts`, `apps/api/src/modules/connectors/oauth.test.ts` | Encrypted revocable connector-grant vault + consent seam is tested (ADR-0032/0033). **Acceptance:** the vault + consent surface is exercised against real Postgres — an OAuth token is sealed at rest (the persisted row holds ciphertext, never the plaintext; a wrong master key fails the AEAD open) yet round-trips through the vault, no capability counts without a stored opt-in, disconnect deletes the token and revokes every grant, the HTTP surface is guarded (401 without a session), and the whole surface is workspace-isolated. Google/Microsoft calendar read + candidate normalization pending (#15). Live OAuth flow: the token exchange (authorization_code + refresh grants, error/shape mapping, `expiresAt`), `freshAccessToken` (fresh→stored, expired→refresh+reseal preserving the refresh token, none→null), the env gates (`oauthClient`/`redirectUriFor`/`masterKeyFromEnv`), and signed-state round-trip / tamper / foreign-key / stale / malformed rejection — all fake-fetch, no network. |
| REQ-011 | Verified | `packages/domain/src/rules/engine.test.ts`, `apps/api/src/modules/automation/automation-rules.integration.test.ts`, `apps/mobile/src/screens/RulesScreen.test.tsx` | Deterministic rules engine (ADR-0005): `matches` (AND over present conditions — note-contains case-insensitive, source, project-empty, start-within half-open, weekday-in; a missing subject field fails a condition needing it), `orderedRules` (by order then id, disabled dropped), `evaluate` (first match wins, `null` on none, `rule:<id>@<version>` provenance), `ruleProvenance`, and `dryRun` (previews every subject without applying). Pure. Persistence + API acceptance: the automation-rules integration test boots the app against real Postgres and asserts CRUD round-trip persists the JSON matcher/action, `updateRule` bumps `version`, `dryRunRules` proposes from the engine yet writes nothing, workspace B can neither read nor mutate A's rules, and `GET /api/automation/rules` returns 401 unauthenticated. Client acceptance: the RulesScreen render test mounts the editor with injected rules/catalog and pins that it renders each rule's matcher/action summary. |
| REQ-012 | Verified | `apps/api/src/modules/ai/llm/null-llm.test.ts`, `apps/api/src/modules/ai/llm/vercel-llm.test.ts`, `apps/api/src/modules/boundaries.test.ts`, `apps/api/src/modules/ai/ai.integration.test.ts`, `apps/api/src/modules/ai/categorize.test.ts`, `apps/mobile/src/screens/TodayScreen.test.tsx` | `LlmPort` (ADR-0029): NullLlm graceful degradation + the single `VercelLlm` adapter; boundaries test confines vendor types. **Acceptance:** the AI edge is exercised end-to-end against real Postgres — when the provider is down the assistant and NL entry both degrade to their deterministic path and never charge. **Categorization batch (#17):** the categorizer parses fenced/strict JSON defensively, canonicalizes a proposed project against `knownProjects` case-insensitively or nulls it (never invented), and the AI integration test proves debit-once / unknown-project-nulled / provider-down / no-credits over the real ledger; the Today render test pins list → propose → Apply with visible provenance and that nothing applies on its own. |
| REQ-013 | Verified | `packages/domain/src/nlentry/parse.test.ts`, `apps/api/src/modules/ai/nl-entry.service.test.ts`, `apps/api/src/modules/ai/ai.integration.test.ts`, `apps/mobile/src/api/nlEntry.test.ts` | Deterministic de/en pre-parser + LLM fallback; always a confirmed draft, never silently persisted. **Acceptance:** the `/api/ai/nl-entry` handler returns a draft and leaves the time-entries table empty (real Postgres) — nothing is written without the user's confirm. |
| REQ-014 | Verified | `apps/api/src/modules/planner/briefer.test.ts`, `apps/api/src/modules/planner/briefing.integration.test.ts`, `packages/domain/src/reporting/standup.test.ts`, `apps/api/src/modules/ai/standup.test.ts`, `apps/mobile/src/screens/ReportsScreen.standup.test.tsx` | AI day-briefing (narrative around domain numbers, plain-template degradation) is tested. **Acceptance:** the credit-priced briefing endpoint runs over the real ledger in Postgres — one AI briefing debits exactly one credit and re-briefing the same plan never double-charges (idempotent per plan), a down provider degrades to the factual deterministic summary and charges nothing, a zero-balance workspace gets no AI and no charge, the metering is workspace-isolated, and the HTTP surface is guarded (401). **Standup core ships:** `buildStandup` (normalize/sort/total), `formatHm`, `standupSlots` + `slotsPreserved` (an AI draft that keeps every number passes, one that alters a number fails — slot integrity), `renderStandupPlain` (AI-free degradation with honest empty-day placeholders). **Standup endpoint ships:** `POST /api/ai/standup` (`LlmStandupWriter`) narrates around the slots, rejects any AI draft that drops/alters a figure (`slotsPreserved`) and debits one credit only on a real AI narrative — the standup unit test pins the degradation contract and the AI integration test proves debit-once / slot-violation-no-charge / provider-down-no-charge / workspace isolation over the real ledger. **Client surface:** the Reports Standup card sends the window's real per-project lines and renders the narrative with its provenance badge (render-tested). |
| REQ-015 | Verified | `apps/api/src/modules/ai/assistant.test.ts`, `packages/domain/src/assistant/grounding.test.ts`, `apps/api/src/modules/ai/ai.integration.test.ts`, `apps/mobile/src/api/assistant.test.ts` | Grounded read-only query tools, defined refusals, no state mutation from chat. **Acceptance:** the credit-priced assistant runs over the real ledger in Postgres — one grounded AI answer debits exactly one credit, a refusal or the deterministic fallback debits nothing, a zero-balance workspace gets no AI and no charge, and the metering is workspace-isolated. **Deepened:** the pure `assistant/grounding` core — `rankFacts` (IDF-weighted overlap, stopwords dropped, rare words outweigh common, stable ties), `selectGroundingFacts` (top-K relevant only), `isOffData` — grounds the model on the most relevant facts and refuses off-data questions without a model call (assistant unit test asserts the model is never called off-data). |
| REQ-016 | Verified | `packages/domain/src/entitlements/derive.test.ts`, `packages/domain/src/entitlements/features.test.ts`, `apps/api/src/modules/billing/entitlements.integration.test.ts` | Pure `deriveEntitlement` state machine + `can()` gating; idempotent webhook-event convergence, derive-on-read service. |
| REQ-017 | Verified | `apps/api/src/modules/billing/payments/stripe/gateway.test.ts`, `apps/api/src/modules/billing/payments/stripe/stripe.integration.test.ts` | Stripe SDK confined to `billing/payments/stripe`; signature-verified idempotent webhook → entitlement log. |
| REQ-018 | Planned | — | Store subscriptions (StoreKit 2 + Play Billing) — Proposed, tracked by [#23](https://github.com/NexusHero/myDevTime/issues/23). |
| REQ-019 | Partial | `apps/api/src/modules/auth/guard.test.ts`, `apps/api/src/modules/boundaries.test.ts`, `apps/api/src/config.test.ts`, `apps/api/src/security-headers.test.ts`, `apps/api/src/authz-sweep.test.ts` | Auth guard, adapter boundaries, and production config validation (secure secret, email-verification lock) are enforced. **Headers baseline (#24):** every response carries nosniff / frame-DENY / no-referrer / minimal Permissions-Policy / COOP (HSTS production-only, CSP deliberately not set to keep the OpenAPI docs route working) — asserted over a booted app. **Authz sweep (#24):** the authz-sweep test walks the composition-root module graph and fails if any HTTP route is neither `AuthGuard`-guarded nor on a justified public allowlist (health/readiness, static status pings, provider discovery, Stripe-signature webhook, capability-token free/busy), with a discovery floor so it can't pass vacuously and a stale-allowlist assertion. Prose deliverables `docs/security/hardening.md` (STRIDE-lite threat table) and `docs/security/prompt-injection-review.md` (per-endpoint ADR-0005 analysis) accompany it. Deferred: CI secret-scanning matrix. |
| REQ-020 | Partial | `apps/api/src/modules/privacy/retention.test.ts`, `apps/api/src/modules/privacy/privacy.integration.test.ts` | Privacy/DSGVO package (Art. 15 export, Art. 17 erasure) — Proposed, tracked by [#25](https://github.com/NexusHero/myDevTime/issues/25). **Privacy module ships (#25):** the integration test proves the Art. 15 export contains only the caller's workspace data, Art. 17 erasure removes workspace + auth rows, retention purge deletes only old soft-deleted rows, and the surface is guarded (401). **DPA + consent docs ship (#25):** `docs/privacy/dpa-no-training-matrix.md` catalogues every external processor (LLM, ASR, Stripe, store IAP, Google Calendar, Jira/Linear/Slack) with data categories, legal basis, DPA/no-training requirement, confining adapter and env-gate; `docs/privacy/consent-points.md` inventories every consent surface against its code reference with a lifecycle diagram and an honest gaps section. Documentation package complete; residual product gaps tracked in the consent doc. |
| REQ-021 | Partial | `apps/api/src/db/ready.integration.test.ts`, `apps/api/src/request-id.test.ts`, `apps/api/src/modules/observability/counter.service.test.ts`, `apps/api/src/modules/observability/metrics.test.ts`, `apps/api/src/modules/observability/observability.interceptor.test.ts`, `apps/api/src/modules/observability/observability.integration.test.ts` | Readiness/liveness probes tested and smoked in CI (`scripts/container-smoke.sh`, ADR-0052). **Traceability + readiness (#26):** a well-formed inbound x-request-id is echoed (junk replaced by the request's own id) and /health/ready reports a real DB ping — 503 problem+json when degraded, never fake-ready. **Metrics (#26):** `GET /api/observability/metrics` (guarded) returns request counts bucketed 2xx/4xx/5xx (fed by a global interceptor whose classification mirrors the RFC-7807 filter), an AI counter surface (`ai.calls`/`ai.creditsSpent`) other modules increment via the exported `CounterService`, and uptime — deterministic in-process registry, unit + integration tested; `docs/ops/runbook.md` documents topology, alerting signals, incident playbooks and backup/restore. Deferred: feeding the AI counters from the ai module, external alerting, rehearsed restore. |
| REQ-022 | Verified | `e2e/tests/shell-auth.spec.ts`, `scripts/container-smoke.sh`, `e2e/tests/golden-paths.spec.ts` | Golden-path acceptance suite lands with ADR-0053: the built app is driven through a real browser against the running stack (app mounts, sign-in works). Both mobile platforms + the 20-green flake gate remain (#27). **Golden paths (#27):** register→Today, timer start/stop, Reports rendering and a keyboard focus-visibility tab-through run in the browser-acceptance job. Deferred: mobile platforms + flake gate. |
| REQ-023 | Partial | `scripts/container-smoke.sh` | The web image's nginx serves the exported PWA (smoked in CI, ADR-0052); App Store / Play Store distribution + staged rollout pending (#28). |
| REQ-024 | Planned | — | Pricing decision — recorded as an ADR before store submission; tracked by [#29](https://github.com/NexusHero/myDevTime/issues/29). |
| REQ-025 | Partial | `apps/api/src/modules/ai/transcription/service.test.ts`, `apps/api/src/modules/ai/transcription/whisper-http.test.ts`, `apps/api/src/modules/ai/transcription/transcription.provider.test.ts` | Meeting transcription pipeline (ADR-0009): the narrow `TranscriptionPort` + `NullTranscription` (always unavailable, `transcribe` rejects) and `planMeetingInsights` gating **consent → Pro → availability** before any ASR call (a fake ASR + Null adapter prove no-consent/not-pro/unavailable → empty, consented+Pro → insights). Vendor ASR confined to the future adapter. **Deferred:** the live ASR adapter (spike #31) + transcript↔entry link + DSGVO lifecycle, with an acceptance-tier test. Live adapter: `WhisperHttp` (multipart `/v1/audio/transcriptions`, `verbose_json` segment→ms mapping, auth header only when a key is set, every failure → `TranscriptionUnavailableError` so text is never fabricated) + the env provider matrix (unset/unknown→Null, self-hosted base without key→adapter, key→OpenAI default base). No real network. |
| REQ-026 | Partial | `packages/domain/src/meetings/transcript.test.ts`, `apps/api/src/modules/ai/transcription/service.test.ts`, `apps/api/src/modules/ai/insights.test.ts`, `apps/api/src/modules/ai/meeting-insights.test.ts` | AI meeting insights (ADR-0005): `transcriptText` (time-ordered flatten, empties dropped), `transcriptFacts` (grounded facts, action-like first), `actionItemProposals` (confirmed-only `ai-proposal`s, only action-like lines, max-capped). `planMeetingInsights` assembles them behind the Pro/consent gates. **Custom prompts (#33):** the optional user focus is threaded into the insight prompt with the grounding rules stated after it (index-asserted so grounding wins). **Transcript-in endpoint (#33):** `POST /api/ai/meeting-insights` returns free deterministic `facts` + confirmed-only `actionItems` and one credit-priced AI `summary` that degrades to the deterministic fact summary (no charge) when the provider is down / no credits — unit + integration tested. Confirmed-task creation uses the existing tasks endpoint — no meetings store exists server-side. **Client UI (#33):** `apps/mobile/src/screens/MeetingsScreen.summarize.test.tsx` proves the Meetings "Summarize a transcript" box calls the endpoint and renders the summary with honest provenance (`ai-proposal` "1 credit used" vs `deterministic` free) plus proposal action items. **Deferred:** live ASR capture (spike #31). |
| REQ-027 | Verified | `packages/domain/src/credits/ledger.test.ts`, `packages/domain/src/credits/allowance.test.ts`, `apps/api/src/modules/billing/credits.integration.test.ts`, `apps/api/src/modules/billing/payments/stripe/stripe.integration.test.ts`, `apps/mobile/src/api/credits.test.ts` | Append-only signed deltas → balance/usage derived by the core; debits idempotent on `operationId`; workspace-isolated. **Credit automation (#148)**: `monthlyCreditAllowance`/`TOPUP_PACKS` deterministic amounts; `grantMonthlyAllowance` + `grantTopUp` grant idempotently (per event / per purchase), verified against Postgres; a Stripe subscription webhook grants the plan allowance end-to-end (once per event, re-grants on a new period). |
| REQ-028 | Verified | `packages/domain/src/attendance/worktime.test.ts`, `packages/domain/src/attendance/break-rule.test.ts`, `packages/domain/src/attendance/coverage.test.ts`, `apps/api/src/modules/worktime/punch-clock.integration.test.ts`, `apps/api/src/modules/worktime/worktime-summary.integration.test.ts`, `apps/api/src/modules/worktime/coverage.integration.test.ts`, `apps/mobile/src/api/worktime.test.ts` | Clock-in/out + breaks, effective-dated schedules, overtime balance, ArbZG §4 break-shortfall, project-coverage reconciliation. |
| REQ-029 | Verified | `packages/domain/src/absences/absence.test.ts`, `packages/domain/src/absences/holidays.test.ts`, `apps/api/src/modules/absences/absences.integration.test.ts`, `apps/mobile/src/api/absences.test.ts`, `packages/design/src/calendar.test.ts` | Half-day counting, allowance + carry-over balance, regional holiday calendars, month-grid marks. |
| REQ-030 | Verified | `packages/domain/src/attendance/report.test.ts`, `apps/api/src/modules/worktime/report/render.test.ts`, `apps/api/src/modules/worktime/report/report.integration.test.ts` | `buildWorktimeReport` (absence days credited against target); PDF signature blocks + typed-cell XLSX rendered only from domain values. |
| REQ-031 | Verified | `packages/domain/src/planner/plan.test.ts`, `packages/domain/src/planner/label.test.ts`, `apps/api/src/modules/planner/planner.integration.test.ts`, `apps/api/src/modules/planner/labeler.test.ts`, `apps/mobile/src/api/planner.test.ts`, `apps/mobile/src/components/planner/TaskInbox.test.tsx`, `packages/design/src/planner.test.ts` | Deterministic `buildDayPlan` + `reviewDayPlan`, versioned plan entity, LLM garnish via `PlanLabeler` port (idempotent credit debit), ghost-block canvas + drag geometry. |
| REQ-032 | Partial | `packages/domain/src/insights/balance.test.ts`, `apps/mobile/src/insights/insights.test.ts`, `apps/mobile/src/insights/checkinStore.test.ts`, `apps/mobile/src/components/instruments/CheckinCard.test.tsx`, `apps/mobile/src/focus/pomodoro.test.ts` | Deterministic `focusStreak` (absence-bridged, in-progress-today graced) + `workloadLoad` (neutral calm/steady/elevated band) over tracked time; the full **Balance** card (design v10) adds `weeklyFocusTrend` (10-week sparkline) + `dailyHoursDistribution` (day-length box plot), composed by `buildBalance` and shown live under Reports → Balance. The weekly OLBI self-report check-in is **local-only** by contract (`checkinStore`, never uploaded — ADR-0060). **Pomodoro/Focus mode**: the deterministic phase machine (`pomodoro.ts` — `advancePhase`, short/long-break cadence, phase durations, remaining-time clamp) drives the shared timer (focus = tracked segment, break = pause); controlled from Today, shown as an Island badge. Only optional native DND still pending (#41). |
| REQ-033 | Verified | `apps/mobile/src/reminder/reminder.test.ts`, `apps/mobile/src/reminder/forgottenTimer.test.ts`, `apps/mobile/src/reminder/idleReturn.test.ts`, `apps/mobile/src/screens/TodayScreen.test.tsx` | Smart Reminder (§D12): the deterministic `shouldRemindToTrack` rule (clocked in, no active timer, past a threshold, not dismissed) behind the dismissible Today nudge. **Forgotten-tracking**: the deterministic `forgottenTimerProposal` rule (a running timer past a plausible threshold → propose Stop / Trim-to-Nh / Keep) behind a dismissible Today card; the trim stops the run at an earlier `endedAt` (`useTimer.punchOut(endedAt?)`). Both evidence-based (the timer's own runtime), no surveillance, nothing auto-corrects. Idle-return detection (design B7) remains pending (#42). **Idle-return (#42):** the pure rule fires only while a timer runs and past the threshold (boundary + clock-skew cases pinned); the Today render test proves the banner appears only with a running timer and offers Keep/Stop without auto-modifying anything. |
| REQ-034 | Partial | `apps/api/src/modules/connectors/oauth.test.ts` | Write-back groundwork: the OAuth vault + refresh flow that a mirror would write through is tested (see REQ-010); the `CalendarPort` is read-only by contract and the `google-calendar` registry entry has no `outbound` capability yet, so one consent-gated scope + adapter method turns this on. **Deferred:** the mirror/idempotency/revoke surface + the live write API (`docs/integrations/calendar-oauth-handback.md`). |
| REQ-035 | Partial | `apps/api/src/modules/ai/export/service.test.ts`, `apps/api/src/modules/ai/export/export-records.integration.test.ts`, `apps/api/src/modules/ai/export/jira-target.test.ts`, `apps/api/src/modules/ai/export/linear-target.test.ts`, `apps/api/src/modules/ai/export/slack-target.test.ts`, `apps/api/src/modules/ai/export/target.provider.test.ts` | Dev-tool export (ADR-0035): the narrow `ExportTargetPort` + `NullExportTarget` (always unavailable) and `runExport` — sends only confirmed items, skips already-exported and same-run duplicate `dedupeKey`s (idempotent, never double-posts), records each outcome with the external id/url, and sends nothing when the target is unavailable (a fake target + Null adapter prove each path). Vendor SDKs confined to the future adapter. **Deferred:** the export ledger persistence + endpoint + client preview, with an acceptance-tier test. **Ledger + API (#44):** the integration test proves records are workspace-isolated, a re-run with the same dedupeKey never double-records (sent rows immutable), the Null target yields honest unavailable outcomes, and the surface is guarded (401). Deferred: live Jira/Linear/Slack adapters. Live adapters (fake fetch, no network): Jira/Linear/Slack request shape + auth, success→external id/url mapping, API-error/`ok:false`→failed, unconfigured→`available()` false; the provider resolves `jira|linear|slack` by env else Null. **Client preview UI (#44):** `apps/mobile/src/components/export/DevToolExportCard.test.tsx` proves the Reports export card lists the ledger, renders a `sent` outcome with its external id/link, and shows the honest "not configured" state for `unavailable` with no fake success. |
| REQ-036 | Verified | `packages/domain/src/tracking/search.test.ts`, `apps/api/src/modules/tracking/entries-search.integration.test.ts`, `apps/mobile/src/screens/TaskScreen.test.tsx` | Deterministic note-match core (`matchesNoteQuery`/`searchEntriesByNote`: case-insensitive substring, blank = no filter). **Acceptance:** `GET /api/tracking/entries?q=` finds entries by note against real Postgres — case-insensitive, LIKE-wildcards escaped (`100%` matches literally), blank returns all, workspace-isolated. The Task screen surfaces the note as the row title and filters the loaded entries with the same core. Note capture on the running timer + timesheet position text ship with REQ-004/009. |
| REQ-037 | Verified | `packages/design/src/calendar.test.ts`, `apps/mobile/src/components/planner/PlannerCalendar.test.tsx` | Month overview (REQ-037, ADR-0013). The deterministic `bookingGapDays` core (`@mydevtime/design`) returns the past weekdays with no booking — never future, never weekend — pinned exhaustively (weekday/weekend via `isWeekdayDate`, cutoff clamp to the month length, all-weekdays-booked → none, future month → none). PlannerMonth renders an activity dot on days with an entry and a booking-gap ring on gap days, and drills Day/Month on tap (Woche⇄Monat). **Acceptance:** the PlannerCalendar month render test exercises the real occurrence → month path. |
| REQ-038 | Verified | `packages/domain/src/budgets/budget.test.ts`, `apps/api/src/modules/billing/billing.integration.test.ts` | Delivered under the Reports analytics work (REQ-005): the pure `burndownProjection` extrapolates run-rate exhaustion; `GET /api/billing/budgets/:id/burndown` samples as-of consumption across the period. **Acceptance:** the burndown endpoint is exercised against real Postgres in the billing integration test. |
| REQ-039 | Planned | — | System quick actions (App Intents / Quick Settings Tile) — Proposed, tracked by [#49](https://github.com/NexusHero/myDevTime/issues/49). |
| REQ-040 | Verified | `apps/mobile/src/components/planner/PlannerDayList.test.tsx` | Classic day list (REQ-040, ADR-0013): the Planner Day view's **Canvas ⇄ List** toggle. `PlannerDayList` renders the day's entries as an accessibility-first list — one labelled row each (type · time span · duration), a **deterministic day subtotal** (Σ of the entries' lengths, ADR-0005), full canvas action parity (row tap → the same drawer), and an honest empty state. **Acceptance:** the render test pins the listed entries, the summed day total (90 + 30 min → 2:00 h), the tap-opens-drawer parity, and the empty state. Per-entry money amounts are a follow-up (need per-block rate resolution). |
| REQ-041 | Verified | `packages/domain/src/estimating/effort.test.ts`, `apps/api/src/modules/tracking/catalog.integration.test.ts`, `apps/mobile/src/components/task/TaskEstimateCard.test.tsx`, `apps/api/src/modules/ai/estimate.test.ts`, `apps/mobile/src/components/task/TaskAiEstimateCard.test.tsx` | Task effort estimation (ADR-0005): `baselineRange` (hours range, not a point), `resolveEstimate` (user number wins with `user` provenance; invalid → `baseline`), `estimateVsActual` (signed delta, tolerance-banded status). **Persistence:** `tasks.category/complexity/estimate_minutes` (migration 0022) set via `PATCH /api/tracking/tasks/:id` — the catalog integration test round-trips the estimate and asserts a foreign workspace **cannot** patch the task (negative isolation). **Form:** the `TaskEstimateCard` render test pins the baseline + estimate-vs-actual readout, the honest "pick a category" prompt, and that saving emits the raw inputs. **AI review ships (#90):** `POST /api/ai/estimate` grounds the LLM in `baselineRange` and clamps the proposal into `[baselineMin·0.5, baselineMax·2]` (no false precision), debits one credit only on a real `ai-proposal`, and degrades to the labelled free baseline midpoint when the provider is down / no credits — unit + integration tested. The `TaskAiEstimateCard` render test proves the honest provenance chip (`ai-proposal` "1 credit used" vs `deterministic` "free baseline") and that Apply reuses `setTaskEstimate` — the proposal never mutates the number on its own. |
| REQ-042 | Partial | `packages/domain/src/autotracker/activity.test.ts`, `packages/domain/src/autotracker/reality.test.ts`, `apps/mobile/src/autotracker/capture.test.ts`, `apps/mobile/src/autotracker/activityStore.test.ts`, `apps/mobile/src/autotracker/dayActivityStore.test.ts`, `apps/mobile/src/autotracker/nativeUsage.test.ts` | Auto-Tracker (ADR-0057/0058/0064, **local-only**): deterministic `summarizeActivity` breakdown + the **reality core** (`reality.ts`: `trackedMs` idle-excluded, `realityDrift` signed tracked−booked, `detectUnbookedGap` single-largest-unbooked-stretch — feeds the Planner reality trace/drift chip K1 + yesterday-healing K3) + the capture port's pure `SpanAccumulator` (now also emitting **timestamped** spans) and first-party `webCapture` adapter + a same-device session buffer (`activityStore`, merged-by-source, cleared on session end) + a **durable per-day reality history** (`dayActivityStore`: timestamped spans keyed by local day, merged-adjacent, pruned to the last 7 days, kept across session-end for the Planner) + the native-usage adapter (`diffUsage` cumulative→span accounting, `nativeUsageCapture` polling), tested with an injected module. The Android `UsageStatsManager` native module is a Dev-Client-only scaffold (`apps/mobile/native/mydevtime-usage/`); the K1/K3 Planner UI are the follow-ups reading this history. |
| REQ-043 | Partial | `e2e/tests/a11y.spec.ts`, `apps/mobile/src/components/instruments/instruments-a11y.test.tsx`, `packages/design/src/contrast.test.ts`, `apps/mobile/src/components/core/focusRing.test.ts`, `apps/mobile/src/components/core/Button.test.tsx`, `apps/mobile/src/components/core/IconButton.test.tsx`, `apps/mobile/src/components/data/Row.test.tsx`, `apps/mobile/src/components/navigation/Tabs.test.tsx`, `apps/mobile/src/components/forms/Input.test.tsx`, `apps/mobile/src/components/forms/Switch.test.tsx`, `e2e/tests/golden-paths.spec.ts` | Accessibility baseline (ADR-0062). The primitive a11y contract is pinned by render tests on every interactive primitive (Button/IconButton/Row/Tabs/Input/Switch/SegmentedControl): role, accessible name, `accessibilityState` (selected/disabled/checked), and the web focus ring. Interactive primitives expose ARIA role/label/state (react-native-web); the graphical instruments announce their value (Gauge signed value + range, Heatmap span + peak, BoxPlot five-number summary, week sparkline trend — render tests pin the labels). **Acceptance:** an **axe-core** pass runs the real app in a browser and fails on any critical/serious WCAG A/AA violation on the sign-in screen and on Today after sign-in, and the sign-in golden path is proven **keyboard-operable** (focus → type → Tab → Enter). Contrast is enforced by `packages/design` (WCAG AA in CI). **Pending (#263):** a visible focus-ring pass + focus management on nav/modals. **Focus ring + management (#263):** the web-only accent outline is unit-tested (native returns {}), the Button render test toggles it via onFocus/onBlur, drawer/dialog/overlay take focus on open (useModalFocus), and the golden-path spec tabs through Today asserting a visible outline. |
| REQ-044 | Verified | `apps/mobile/src/onboarding/OnboardingGate.test.tsx`, `apps/mobile/src/onboarding/OnboardingGate.api.test.tsx`, `apps/mobile/src/onboarding/onboardingStore.test.ts`, `apps/mobile/src/api/tracking.test.ts`, `apps/api/src/modules/preferences/preferences.integration.test.ts` | First-run onboarding flow + gate ship (ADR-0036). **Projects persist** via the client `createProject` (`POST /api/tracking/projects`, audit H8; backend endpoint integration-tested under REQ-001). **Acceptance:** the onboarded flag is durable + cross-device via the server `onboarded` preference — the gate reconciles against it (the API gate test pins onboarded → workspace, not → flow) and the flag round-trips through the preferences endpoint against real Postgres (audit M11 fixed). Local demo (no API) falls back to the local flag. |
| REQ-045 | Done | `packages/domain/src/reporting/export.test.ts`, `apps/mobile/src/reports/exportCsv.test.ts`, `apps/api/src/modules/tracking/export/reports-pdf.test.ts`, `apps/api/src/modules/tracking/reports-export.integration.test.ts`, `apps/mobile/src/api/reportsExport.test.ts`, `apps/mobile/src/screens/ReportsScreen.export.test.tsx` | Reports/analytics export (REQ-045, distinct from the timesheet export REQ-009). The deterministic `reportToCsv` core turns the Reports view-model into labelled, RFC 4180-escaped CSV — hours/money to 2dp, utilization integer %, a comma/quote in a name never breaks a column, byte-identical per input, empty report → headers only (ADR-0005). The PDF rendition: the `reports-pdf` serializer renders the same view-model server-side (unit-tested), `POST /api/tracking/reports/export?format=pdf` is guarded + bounded (integration test: 401 without a session, deterministic bytes), and the client `fetchReportsPdf` posts the live `ReportsData` and downloads it (its test pins the route/method/body; the ReportsScreen export test pins the live/native-degrade control). |
| REQ-046 | Verified | `packages/design/src/planner.test.ts`, `apps/mobile/src/planner/calendarMonth.test.ts`, `apps/mobile/src/components/planner/PlannerCalendar.test.tsx` | Planner utilization aggregation (ADR-0005): the whole load math is a tested pure core in `@mydevtime/design` — `dayLoad` (prio-weighted planned hours/day → Month heat) and `weekIntensity` (a week's load bucketed against the weekly target into 0–3 levels → Year strip, exhaustively pinned: idle/light/steady/heavy boundaries + non-positive load/target). The client `buildMonthDays`/`buildYearMonths` delegate every figure to that core; empty input → honest empty calendar. **Acceptance:** the `PlannerCalendar` render test renders the Year view straight from the aggregation — a single 2 h occurrence shows "2h", empty months "—". |
| REQ-047 | Partial | `packages/domain/src/smartadd/parse.test.ts`, `apps/api/src/modules/ai/insights.test.ts`, `apps/mobile/src/api/smartAdd.test.ts` | Smart-Add typed quick-add (v13 K6, ADR-0065): deterministic `parseEntry` (task/meeting/absence/travel/private + times + day + project/ticket hint, de/en; weak parse → `needsAi`); the Stage-2 `SmartAddService` rewrites a vague phrase and re-parses it through the same core (never bypasses, ADR-0005); the client parser + draft→times helper; the Today Smart-Add sheet confirms before writing. **Acceptance pending:** a client render test of the sheet + the smart-add endpoint integration. |
| REQ-048 | Partial | `packages/domain/src/economics/effective-rate.test.ts` | Effective-rate truth (v13 G2, ADR-0065): `perHourRate` exact BigInt division + `effectiveRate` (nominal ÷ billable, effective ÷ all-tracked, utilization). Surfaced on Reports. **Acceptance pending:** Reports render test. |
| REQ-049 | Partial | `packages/domain/src/economics/overtime-forecast.test.ts` | Overtime compound (v13 G3, ADR-0065): running balance fold + OLS forecast + pattern note. Surfaced on Reports via `useOvertimeTrend`. **Acceptance pending:** Reports render test. |
| REQ-050 | Partial | `packages/domain/src/economics/week-price.test.ts`, `apps/mobile/src/planner/weekPrice.test.ts` | Price of the week (v13 G1, ADR-0065): intensity solver (sustainable/balanced/dense) — active days, per-day load, overtime, revenue, strain. Surfaced on **Reports** (from tracked totals) and as the Planner **in-canvas panel** after Fill-week (from the planned blocks via `priceWeekFromBlocks`). **Acceptance pending:** a panel render test. |
| REQ-051 | Partial | `packages/domain/src/travel/travel.test.ts`, `apps/mobile/src/travel/travelForm.test.ts` | Travel entry type (v13 G4, ADR-0065): `priceTravel` (reduced fraction + per-km, train=worktime), `returnTrip`, `nextLegStart`, `frequentRoutes`. The **route-card drawer** (`TravelEntry` on Today) collects route/distance/mode, shows the deterministic worktime + mileage preview, offers the return-trip nudge + magnetic chain, and creates a real travel-tagged entry; Smart-Add also classifies travel phrases. **Acceptance pending:** a drawer render test. |
| REQ-052 | Partial | `packages/domain/src/attendance/statement.test.ts`, `apps/api/src/modules/worktime/report/statement-render.test.ts` | Monthly work-time statement — "real punch clock" (v13 X, ADR-0065): `buildMonthlyStatement` (begin/pause/end, ±, cumulative from carryover to closing, absence rows, audit note) + `monthlyStatementToPdf` + `GET /api/worktime/statement` + the WorkTime export button. **Acceptance pending:** the statement endpoint integration against real Postgres. |
| REQ-053 | Partial | `packages/domain/src/estimating/quote.test.ts` | Quote-from-history (v13 KI2, ADR-0065): `estimateFromHistory` distribution (median/quartiles/p90) + buffered suggestion; null with no history. **Acceptance pending:** the quote surface + its render test. |
| REQ-055 | Partial | `packages/domain/src/capacity/plannable.test.ts`, `packages/design/src/palette.test.ts`, `packages/design/src/contrast.test.ts`, `apps/mobile/src/planner/capacityTrace.test.ts` | Capacity honesty — one person, one timeline (v14 §F, ADR-0066): `committedMinutes` (overlaps merged), `dayCapacity` / `weekCapacity` (plannable = target − life/protected, clamped; "KW32 nur 24h"), `overbookedMs` (against the *true* plannable), and the sage `--life` token (AA-Large on surface in all six accent × mode combos). **On-screen:** the Planner **capacity head-trace** — `weekCapacityFromBlocks` shapes the real canvas blocks into `weekCapacity`, the header strip shows plannable vs target with the sage life/protected segment, `life` blocks render in `--life` and are excluded from planned work. **Deferred:** persisting the `life` entry type (create/edit) + partner Free/Busy share, and capacity-aware Fill-week — acceptance criteria in ADR-0066, each with an acceptance-tier test. |
| REQ-056 | Partial | `packages/domain/src/roles/visibility.test.ts`, `apps/mobile/src/roles/RoleContext.test.tsx` | Roles & tiers as a visibility preset (v14 §R, ADR-0066): `isModuleVisible` / `visibleModules` resolve (role × Pro × Family add-on × user overrides) → visible modules, enforcing the hard floors — Stempler (Free) never sees money/clients/billing, Health visible in every tier (never paywalled, never hideable), Family only with the add-on, and no override can breach a floor. **On-screen:** `RoleProvider`/`useVisibility` apply the role via the resolver (employee hides money, Health always shows, switching role re-derives live); **Settings** carries the role picker; **Reports** hides the Revenue & Budget view for an Employed role. **Deferred:** persisting the role choice + gating the remaining money surfaces + per-module toggles. |
| REQ-057 | Partial | `packages/domain/src/protection/protection.test.ts`, `apps/mobile/src/components/planner/PlannerEntryDrawer.test.tsx` | Protection flag 🛡 (v14 D14, ADR-0066): `isProtectedAt` (half-open block test), `partitionByProtection` (hold nudges/requests during protection, deliver the rest), `buildDigest` (**one** digest with per-kind counts — nothing lost), `transitionPromptDue` (ask once, only when punched in; never auto-punch-out). No time-tracking math by construction — the flag can only route communication. **On-screen:** the Planner entry drawer carries the 🛡 **toggle** (existing entries only — meeting/booked/life, not ghost/break; fires `onProtect`), and a protected block also reduces the §F plannable capacity. **Deferred:** the Today digest card + Island transition prompt (held-nudge runtime) + Outlook "Busy" adapter, with a client/adapter acceptance test. |
| REQ-058 | Partial | `packages/domain/src/insights/health.test.ts`, `apps/mobile/src/reports/balanceRow.test.ts` | Health & Balance (v14 §H, ADR-0066): `personalBaseline` (own mean + population spread over ≥4 periods; null below that) and `compareToBaseline` (band relative to the person's own spread — the **same 50h week** reads `typical` for a 50h-baseline person and `above` for a tight 40h-baseline person; H3 "never a fixed threshold"), plus `balanceRow` (Work/Protected/Free of waking time, Free is the honest remainder; H1). **On-screen:** the Reports **Balance view** — `weeklyBalance` feeds `balanceRow` (Work/Protected/Free stacked bar, sage protected) and `compareToBaseline` over the 10-week trend ("vs your own usual" band; H3). Protected is 0 until the life/🛡 persistence lands. **Deferred:** entry attachments (H2) + Slack **WHEN-only** (H4), each with an acceptance-tier test. |
| REQ-059 | Partial | `packages/domain/src/planner/banner.test.ts`, `apps/mobile/src/components/planner/ContextBanner.test.tsx` | Simplification pass (v14 §M2, ADR-0066): `pickBanner` returns the single highest-priority banner by the fixed order Conflict > Price > Healing > Note (order-independent, variant ties break toward the earlier candidate, the winner's full payload preserved, `null` when none); `BANNER_PRIORITY` is the binding order. **On-screen:** the client `ContextBanner` (one component, `variant` prop) renders title/body/actions per variant, and PlannerScreen routes its healing + note banners through `pickBanner` so at most one shows — "max. EIN Banner" made law in code. **Deferred:** the Island/header simplifications (M1/M3/M4) and a conflict-variant banner once week-level overbooking surfaces, with a client acceptance test. |
| REQ-060 | Partial | `packages/domain/src/recurrence/recur.test.ts`, `apps/api/src/modules/recurrence/service.test.ts`, `apps/api/src/modules/recurrence/recurrence.integration.test.ts`, `apps/mobile/src/api/recurrence.test.ts`, `apps/mobile/src/planner/recurring.test.ts`, `apps/mobile/src/components/planner/RecurrenceEditor.test.tsx`, `apps/mobile/src/components/planner/PlannerEntryDrawer.test.tsx`, `apps/mobile/src/components/planner/PlannerNewEntryDialog.test.tsx`, `apps/mobile/src/planner/calendarMonth.test.ts` | Recurring entries (v17 §F4, ADR-0067): `expandRecurrence` (none = start only; daily = weekdays; weekly = same weekday; monthly = same day-of-month, **short months skipped** so a 31st never drifts; honours until/count, capped), `isOccurrence`, `truncateBefore` (the "series from here" split ends the original the day before, Outlook-style), `describeRecurrence` (the ↻ row label). Pure date-string math, no clock. **Persistence + API ship:** the `recurrence` module stores one rule per series (`recurring_entries`, workspace-scoped, migration 0018) and projects occurrences at read time (`GET /api/recurrence/occurrences`) with create/list/delete + the Outlook `POST /:id/truncate`; `seriesToOccurrences`/`rowToRule` are unit-tested locally and the integration test covers create/list, projection, the truncate split, workspace isolation and the auth guard. **The client data seam ships:** `api/recurrence` (parsers + create/list/occurrences/delete/truncate over an injected fetch) and the pure `occurrencesToBlocks` mapper (day-column placement, START_HOUR offset, kind map, off-week drop) are unit-tested. The **drawer ↻ editor** ships: `RecurrenceEditor` (freq + end, `describeRecurrence` label) and the drawer's "Make recurring" → `createSeries` are render-tested (ADR-0027). **Occurrence rendering ships:** `useWeekOccurrences` fetches the shown week (empty without an API) and the tested `occurrencesToBlocks` places them on the canvas as read-only ↻ ghosts. **The v19 New-Entry dialog** (`PlannerNewEntryDialog`, render-tested) collects a Task/Life draft and the Planner persists it via `createSeries` at the next free slot (real projects from `useCatalog`, quick-add via `createProject`); the series stores **`priority` + `note`** (migration 0021, round-tripped in the service + integration tests), and `calendarMonth` carries the real priority and **excludes `life` from the work-load** (unit-tested). **Deferred:** the this-vs-series edit prompt + Fill-week honouring series, with a client acceptance test. |
| REQ-061 | Verified | `packages/domain/src/economics/plan-variance.test.ts`, `apps/api/src/modules/tracking/catalog.integration.test.ts`, `apps/mobile/src/components/reports/PlanVarianceChip.test.tsx` | Plan-vs-realized revenue (v17 §K4, ADR-0067): `planVsRealized` — signed `deltaMinor`, rounded `variancePct`, `over`/`on`/`under` within a tolerance (default 2 %, overridable), `null` % on a zero plan (status by sign). Pure integer-minor math, no AI/forecast. A nullable `fixedFeeMinor` on `projects` (minor units, migration 0019) persists the expected revenue (integration test round-trips create/update/clear), and the **`PlanVarianceChip`** renders `planVsRealized(fixedFeeMinor, cost-to-date)` on fixed-fee projects — signed, tolerance-banded, tone over=warn/under+on=calm, hidden when there is no fixed fee (render-tested, ADR-0027). |
| REQ-062 | Partial | `packages/domain/src/autotracker/timesheet-drafts.test.ts`, `apps/mobile/src/today/shutdown.test.ts`, `apps/mobile/src/api/timer.test.ts` | Timesheet drafts (v17 §K/KI6, ADR-0067): `timesheetDrafts` — every unbooked active tracker stretch ≥ the floor becomes a bookable draft (start/end/duration + dominant source), idle excluded, booked subtracted, `recoveredMs` summed; a review queue, never auto-booked. The client presenter `todayShutdown` exposes the draft list and `createEntry` books a draft's window as a real manual entry (`POST /entries`); the Today "Review your tracked day" card wires Book / Book all on the user's tap. **Deferred:** the grounded-LLM title phrasing + per-draft project assignment, with a client/AI acceptance test. |
| REQ-063 | Partial | `packages/domain/src/shutdown/summary.test.ts`, `apps/mobile/src/today/shutdown.test.ts` | Shutdown / Feierabend ritual (v17 §K5, ADR-0067): `shutdownSummary` — booked, tracked, `unbookedMs` (tracked − booked, clamped ≥ 0), open-draft count, tomorrow-first, and `clean` (no open drafts and no unbooked reality). Pure aggregation. The client presenter `todayShutdown` folds real reality spans + booked entries into the summary and resolves the honest `idle`/`clean`/`review` state (idle-source spans excluded, sub-floor stretches not drafted); the Today "Close the day" card renders it. **Deferred:** a persisted per-day "closed" state + a real tomorrow-first source, with a client acceptance test. |
| REQ-064 | Partial | `apps/mobile/src/planner/layer.test.ts`, `apps/mobile/src/components/planner/PlannerEntryDrawer.test.tsx`, `packages/design/src/people.test.ts`, `packages/design/src/hash.test.ts`, `packages/domain/src/sharing/freebusy.test.ts`, `apps/api/src/modules/sharing/service.test.ts`, `apps/api/src/modules/sharing/sharing.integration.test.ts`, `packages/domain/src/calendarsync/merge.test.ts`, `apps/api/src/modules/calendarsync/service.test.ts`, `packages/domain/src/photoimport/schedule.test.ts`, `apps/api/src/modules/photoimport/service.test.ts` | Family-market layer filter + stroke-shield icon (v17 §F6.5 + icon rule, ADR-0067): `inLayer`/`PLANNER_LAYERS` — Both shows every kind, Work hides `life`, Life shows only `life`, default Both first; the Planner header wires the Work/Life/Both pills to it (shown blocks filtered, capacity/price read the full set). The drawer protection row renders the `shield` stroke-SVG Icon, never the 🛡 emoji (asserted `not.toContain('🛡')`). **§F6 person shades ship:** `personShade`/`personShadeIndex` assign a deterministic **sage** shade of the `life` tone via a shared FNV-1a `hash` (in-range, theme-independent, distinct shades, slot 0 = the plain `life` token; no rainbow palette). **The one-link partner-light share ships:** `toFreeBusy`/`freeGaps` redact an owner's blocks to a Free/Busy-only `FreeBusySlot` (no title/project/note field — a leak is unrepresentable; a 🛡 block reads busy), unit-tested down to "a secret string never survives serialization"; `projectFreeBusy` clamps a running timer and clips busy to the window; the `sharing` integration test (real Postgres) proves the **negative isolation** (a private note never crosses the public `GET /api/sharing/:token/freebusy`), workspace scope, revocation, and the public-read / authenticated-management guard split (`partner_shares`, migration 0020). **Google/Apple calendar sync ships as a port:** `mergeCalendar` diffs external events vs imported blocks into new/changed/orphaned proposals keyed on uid (never writes; drops empty/inverted events; a re-run over the applied state proposes nothing), and `planImport` gates on consent then availability over the narrow `CalendarPort` (a `NullCalendar` and a fake in-memory adapter prove no-consent → nothing, unavailable → empty plan, consented → ghost proposals). **KI5 photo/mail import ships as a port:** `toSeriesProposals` shapes AI-extracted lessons into weekly `RecurrenceRule` series proposals (`ai-proposal` provenance, `confirmed: false`, never booked; drops invalid lessons — bad weekday, non-positive length, past-midnight, empty title), and `planPhotoImport` gates on Pro → consent → availability over the narrow `ScheduleImportPort` (a `NullScheduleImport` and a fake vision adapter prove not-pro/no-consent/unavailable → nothing, and Pro+consented → ghost series with a one-credit cost charged only on booking). **Deferred:** the live vision + Google/Apple provider adapters (integration spike) and the client ghost-confirm UI — each with an acceptance-tier test. |
| REQ-065 | Done | `packages/domain/src/wellbeing/dayReview.test.ts`, `packages/domain/src/wellbeing/baseline.test.ts`, `apps/api/src/modules/ai/companion.test.ts`, `apps/api/src/modules/wellbeing/service.test.ts`, `apps/api/src/modules/ai/ai.integration.test.ts`, `apps/api/src/modules/ai/companion.integration.test.ts`, `apps/mobile/src/components/today/EveningCompanionCard.test.tsx` | Evening Companion / wellbeing (ADR-0069/0005): `reviewDay` bands the day's load and emits structured `WellbeingSignal`s; `computeBaseline` gives a personal normal band + trend + `consecutive-heavy-days`/`weekday-overbook` flags (short history → wide band, no false flags) — 100 % lines. `POST /api/ai/evening-companion` runs the pure core free then a grounded narration; the integration test proves it debits one credit **only** on a real `ai-proposal` and degrades free when the provider is down. The `wellbeing` service test pins the `wellbeing_days` persistence: idempotent upsert per (workspace, user, day), newest-first history read with derived weekday, and workspace isolation (a foreign workspace never sees another's series). The companion integration test proves the endpoint persists today's `loadScore`, calibrates the baseline over the real persisted series, and reads the real worktime feed for overtime + break-shortfall. The Today `EveningCompanionCard` render test pins the band + week-trend + honest provenance (`ai-proposal` "1 credit used" vs `deterministic` free) and the proposal-only suggestion (nothing booked). **Deferred:** a consented mood store (mood signal stays honestly omitted). |
| REQ-066 | Partial | `packages/domain/src/issueimport/issues.test.ts`, `apps/api/src/modules/issueimport/github-import.test.ts`, `apps/api/src/modules/issueimport/azure-import.test.ts`, `apps/api/src/modules/issueimport/service.test.ts`, `apps/api/src/modules/issueimport/issueimport.integration.test.ts`, `apps/mobile/src/components/issues/IssueImportCard.test.tsx` | Ticket import (ADR-0070/0005): the domain `toTaskProposals` maps ExternalIssue → CandidateTaskProposal with dedup (vs already-imported + in-batch), closed-filter and deterministic order (100 % lines); the GitHub + Azure DevOps adapters against a fake fetch (request shape/auth → ExternalIssue, HTTP-error → unavailable, empty → []); the preview endpoint integration (401 without session, 409 without consent/token, honest `unavailable`/`no-consent`, **writes nothing**); the client `IssueImportCard` render test (proposals render, `no-consent` shows the honest reason with zero task-creates, select + Import creates exactly one task per selection). **Deferred:** an imported-issue store; the live Azure DevOps OAuth grant. |
| REQ-054 | Partial | `apps/api/src/modules/ai/insights.test.ts`, `apps/mobile/src/api/aiInsights.test.ts`, `apps/mobile/src/insights/quoteFacts.test.ts` | Grounded AI difference-makers (v13 KI1–KI4, ADR-0065): the `LlmInsights` grounded proposals (coach/quote/invoice/meeting) — phrase-only, refuse off-data, degrade, one credit per proposal; the `/api/ai/insight` endpoint + client parser; the generic `InsightCard`. **On-screen surfaces shipped:** Drift-Coach (KI1, Reports), Quote-from-history (KI2, ProjectScreen — grounded in the project's task-duration history via `quoteFacts`), Invoice translator (KI3, InvoiceDrawer — over the selected lines' notes), Meeting follow-ups (KI4, MeetingsScreen — the `MeetingNote` card grounds `meetingNotesFacts` over the user's own typed notes; `packages/domain/src/meetings/notes.test.ts`). **Deferred:** ASR **auto-capture** (transcript) until the ADR-0009 spike lands — the same `meetingNotesFacts` core will feed it. **Acceptance pending:** the insight endpoint integration + a card render test. |

---

## Keeping this current

When you change a requirement, in the **same PR**:

1. Update the register row in `docs/architecture.md` §1.
2. Update this matrix: add the row (new REQ), or move a REQ from **Planned** →
   **Partial** → **Verified** as tests land, naming the exact test paths.
3. `./test.sh` runs `check:req-coverage`, which fails if a register REQ has no
   row here or if a named test path no longer exists.
