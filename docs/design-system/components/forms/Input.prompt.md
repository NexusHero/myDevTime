Text field with focus glow ring. Set `mono` for anything numeric (rates, durations) so it aligns with the rest of the "numbers are the product" system.

```jsx
<Input label="Hourly rate" mono value={rate} onChange={setRate} />
```
