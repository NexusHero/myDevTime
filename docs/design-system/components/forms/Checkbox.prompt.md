Multi-select checkbox, 6px radius (block radius, not pill — distinguishes it from Switch at a glance).

```jsx
<Checkbox checked={includeWeekends} onChange={setIncludeWeekends} label="Include weekends" />
```
