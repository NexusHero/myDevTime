Toggle for booleans — rule toggles, notification prefs, dry-run flags. Slides with `--ease-out` + `--dur-med`, no bounce.

```jsx
<Switch checked={remindersOn} onChange={setRemindersOn} label="Break reminders" />
```
