Linear gauge centered on zero (ux-vision §2.5) — never a ring (rings are reserved for MATCH/budget), so overtime reads distinctly.

```jsx
<OvertimeGauge hours={4.5} />
<OvertimeGauge hours={-2.25} />
```
