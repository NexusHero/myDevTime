Underline tabs — accent underline on the active tab, no pill/background treatment.

```jsx
<Tabs items={[{value:'week',label:'Week'},{value:'month',label:'Month'}]} active={range} onChange={setRange} />
```
