One floating pill, same information architecture on every platform (ux-vision §2.3). Collapsed = glance; tap expands to quick actions. Morphs corner radius + size, never a hard swap.

```jsx
<Island running elapsed="00:42:11" punched actions={[{label:'Pause'},{label:'Ausstempeln'}]} expanded={open} onToggle={() => setOpen(!open)} />
```
