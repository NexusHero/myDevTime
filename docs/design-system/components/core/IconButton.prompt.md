Circular icon-only control, 44pt touch target minimum even at `sm` visual size — for command bars, the Island, and row actions.

```jsx
<IconButton icon={<PlusIcon/>} label="Add entry" onClick={openQuickAdd} />
<IconButton icon={<BellIcon/>} label="Notifications" active variant="filled" />
```
