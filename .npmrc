# Fail fast on lockfile drift in CI; keep installs deterministic.
engine-strict=true
# Internal packages are consumed as source (see ADR-0014) — hoist nothing surprising.
strict-peer-dependencies=false
