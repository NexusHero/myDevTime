# Copy to .env for local development (.env is gitignored — never commit secrets).
NODE_ENV=development
PORT=3000
LOG_LEVEL=info

# PostgreSQL — required in production; readiness/integration tests skip when unset.
DATABASE_URL=postgres://mydevtime:mydevtime@localhost:5432/mydevtime
