// Placeholder so the workspace + typecheck resolve. The real backend skeleton
// — module layout (auth · tracking · sync · automation · ai · billing),
// configuration, /health, persistence (+ its ADR), and OpenAPI — lands in
// issue #3. Do not build features here before that.
export const API_PLACEHOLDER = true
