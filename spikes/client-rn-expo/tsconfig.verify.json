{
  "//": "Typecheck ONLY the pure logic cores (no RN/native deps) — the machine-verified half of the spike. The RN adapters (db.ts, persist.ts) and the .tsx scaffold import native modules and are checked by the human via `npx expo install` + `tsc` (see README).",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": []
  },
  "include": [
    "src/timer/elapsed.ts",
    "src/offline/outbox.ts",
    "src/dashboard/keymap.ts",
    "src/canvas/layout.ts"
  ]
}
