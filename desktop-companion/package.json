{
  "name": "mydevtime-desktop-companion",
  "version": "0.0.0",
  "private": true,
  "description": "myDevTime Auto-Tracker companion for macOS/Windows — local per-app capture reusing @mydevtime/domain (ADR-0059). Standalone: NOT a pnpm-workspace member; build it locally.",
  "main": "main.cjs",
  "scripts": {
    "start": "electron ."
  },
  "dependencies": {
    "@mydevtime/domain": "file:../packages/domain",
    "active-win": "^8.2.1",
    "electron": "^31.0.0"
  }
}
