{
  "name": "@mydevtime/local-db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Offline-first local store (ADR-0040): a thin, workspace-scoped SQLite repository — rows in/out, no math. The deterministic domain (packages/domain) computes every number on top of these rows, so offline == online. Vendor SQLite drivers live behind the LocalDb port.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
