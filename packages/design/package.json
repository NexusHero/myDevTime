{
  "name": "@mydevtime/design",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Design tokens + theme resolver — the settled visual language (docs/design/ux-vision.md §4) as pure, platform-agnostic TypeScript. Consumed by the RN + react-native-web client (ADR-0004). No React, no RN imports.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
