# TEMPLATE — do NOT commit real values. Copy to k8s/api/secret.yaml (gitignored)
# and fill in, or create the Secret out-of-band:
#   kubectl create secret generic api-secrets \
#     --from-literal=AUTH_SECRET="$(openssl rand -base64 48)" \
#     --from-literal=AUTH_BASE_URL=https://mydevtime.com \
#     --from-literal=TRUSTED_ORIGINS=https://mydevtime.com
apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
type: Opaque
stringData:
  AUTH_SECRET: __REPLACE_WITH_32+_CHAR_RANDOM_SECRET__
  AUTH_BASE_URL: 'https://mydevtime.com'
  TRUSTED_ORIGINS: 'https://mydevtime.com'
