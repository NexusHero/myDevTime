# TEMPLATE — do NOT commit real values. Copy to k8s/postgres/secret.yaml
# (gitignored) and fill in, or create the Secret out-of-band:
#   kubectl create secret generic postgres-secret \
#     --from-literal=POSTGRES_USER=mydevtime \
#     --from-literal=POSTGRES_PASSWORD="$(openssl rand -base64 24)" \
#     --from-literal=POSTGRES_DB=mydevtime
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  POSTGRES_USER: mydevtime
  POSTGRES_PASSWORD: __REPLACE_WITH_STRONG_PASSWORD__
  POSTGRES_DB: mydevtime
