node_modules/
dist/
coverage/
pnpm-lock.yaml
spikes/

# Generated by drizzle-kit — do not reformat (regeneration would flip it).
apps/api/src/db/migrations/

# Generated by the user-test harness/renderer (docs/design/user-tests/*): the
# JSON is written by JSON.stringify and the HTML is a throwaway render step —
# re-running the reproducer would flip prettier's formatting, so it is excluded.
docs/design/user-tests/**/results.json
docs/design/user-tests/**/render.html

# Markdown is hand-authored prose with carefully aligned tables (arc42 docs,
# ADRs, roadmap). Prettier pads table cells to equal width, which bloats the
# source and fights hand-editing. Docs integrity is enforced instead by the
# deterministic docs-staleness gate (scripts/check-docs.mjs, SKILL §1.5).
*.md
